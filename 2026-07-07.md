# 데일리 작업로그 — 2026-07-07 (화)

> 전날(2026-07-07) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 7개 프로젝트, 352개 파일 변경.

## howmuch.go — 시세 추정 백테스트 + 원천 캐시 대량 재생성 (144개 파일)
건물 시세 검색 서비스(반경 500m 임대 월세 시세 + 적정 매매가 추정)의 정확도 점검이 이날의 중심이었다. `mode: sale` 백테스트를 두 차례(09:34·09:40) 돌려 강남·서초 일대 여러 필지의 추정가를 실거래가와 대조하고, 신뢰도 등급별 적중률(캘리브레이션)까지 측정했다. 이 과정에서 네이버 매매·월세 호가, 국토부 실거래, 건축물대장, 지오코딩 캐시가 수십 개 필지 단위로 새로 수집·갱신됐고, 대시보드·리포트 미리보기 스크린샷(`cache/_shot`)도 함께 재생성됐다.

## sise-web — 인증·화이트리스트 / 임대시세 신뢰도 개선 설계 2건 + 엔진 캐시 갱신 (117개 파일)
howmuch.go의 웹앱 계열 프로젝트로, 이날은 두 개의 기능 조각을 TDD 구현계획 + 설계문서로 확정했다. 하나는 카카오 OAuth 로그인 + 수동 승인 화이트리스트(pending/approved/admin)로, 입력 검증·사용자별 레이트리밋·job 소유권(타인 job은 존재를 숨겨 404) 방어까지 담은 6개 태스크 계획이다. 다른 하나는 임대시세 비교군 신뢰도 개선으로, 반경을 50~2000m로 세분화하고 ±20% 중앙값 드리프트로 '다른 상권 혼재'를 감지해 확장을 멈추며, 1층과 2층+가 각자 다른 반경을 채택하도록 하는 엔진 리팩터링 설계다. 함께 엔진 실거래·대장 캐시가 대량 갱신됐고, Plan 2b 착수를 위한 보안 이월 항목(PLAN2-TODO)도 정리했다.

## building sns — '서류·현장 불일치' 카드뉴스 1세트 제작 (45개 파일)
빌파고(원빌딩부동산중개) SNS용 부동산 리포트 카드뉴스를 새로 만들었다. 주제는 '계약 전 실사 — 등기부·건축물대장·현장이 서로 다를 때'로, 표지·본문·비교·숫자·체크리스트·CTA까지 10장을 구성했다. 후면·옥탑 판넬 증축의 무허가 리스크(이행강제금·양성화), 면적 불일치가 부르는 매도인–매수인 분쟁, 계약 전 실측 권유 등을 담았다. cards.json → 검토 반영본(revised) → 최종본(cards-final) 순으로 확정한 뒤, 인스타·스레드용(4:5)과 카카오톡 소식용(가운데 정렬 안전형) PNG를 각 10장씩, 본문.md까지 자동 생성했다.

## Building scope — '2종7층 구역' 층수 제한 반영 (19개 파일)
사전 규모검토 엔진(정확도 100%를 해자로 삼는 프로젝트)에 서울 '제2종일반주거지역(7층이하)' 지정구역의 층수 캡을 새로 반영했다. 서울 열린데이터광장 SHP를 필터·좌표변환해 약 1,484개 폴리곤의 지형도면 지오데이터(GeoJSON)를 만들고, 외부 의존 없는 raycast PIP로 필지가 지정구역에 드는지 판정하는 `lib/zone-floors.js`를 추가했다. korean-law MCP로 국토계획법 시행령 별표5·서울시 도시계획 조례 §33 원문을 대조해 인용을 조항 번호까지 정확히 달았고, vworld·zoning·calculate·buildability·rules 등 계산·검증 계층 전반과 검증 스크립트(verify-zone7cap 등)를 캡 발동/비발동 시나리오에 맞춰 갱신했다.

## diwolbu web — 에디토리얼 일일 발행 + 프로덕션 배포 마무리 (14개 파일)
AI 구독 관리·절약 포털의 콘텐츠와 배포를 정리한 날이다. 에디터 4명 체계로 뉴스·꿀팁·칼럼·프로모션 콘텐츠(JSON)를 새로 발행했고, editorial-daily 배치 스크립트도 함께 손봤다. 인프라 쪽으로는 Vercel 프로덕션 배포를 연결(GitHub push 시 자동 배포)하고 Supabase 실제 인증을 붙이는 흐름을 마무리했으며, 배포 도메인 등록·소셜 로그인 provider 설정 등 남은 셋업 절차를 supabase-setup-guide와 TODO에 반영했다.

## team ERP — Plaud 녹음 자동적재 배관 + 규칙기반 자동분류 (12개 파일)
회사 AI TF의 ERP에 Plaud Note 녹음을 자동으로 받아 넣는 파이프라인을 구축했다. 흐름은 Plaud → Zapier(웹훅 + 공유 시크릿) → Supabase Edge Function `plaud-ingest` → 수신함(plaud_inbox)이며, 배포·스모크 테스트·Zapier 필드 매핑 절차를 런북 문서로 정리했다. 나아가 전사문이 외부로 나가면 안 된다는 보안 원칙에 맞춰, 외부 LLM 없이 부동산 도메인 키워드만으로 미팅/회의/교육을 분류하는 규칙기반 분류기(`classify.ts`)와 분류결과 저장 마이그레이션(0009), 수신함 화면(PlaudInboxPage·실시간 훅)을 추가해 확신이 높은 건은 해당 모듈로 자동 승격되게 했다.

## (루트 파일) — .claude 홈 D드라이브 이전 스크립트 (1개 파일)
C: 용량 확보를 위해 사용자 홈의 `.claude` 폴더(세션 기록·설정, 약 3GB)를 D:로 옮기고 원경로에 정션을 만드는 PowerShell 스크립트(`move-claude-home.ps1`)를 작성했다. 복사(원본 무손상) → 원본 잠금 확인(이름변경) → 증분 동기화 → 정션 생성 순으로 안전하게 진행하고, 정상 동작 확인 후 `.old` 폴더를 지워 3GB를 회수하도록 구성했다.
