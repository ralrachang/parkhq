# 데일리 작업로그 — 2026-06-18 (목)

> 전날(2026-06-18) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 5개 프로젝트, 70개 파일 변경.

## building sns — 빌딩 담보대출 카드뉴스 1세트(9장) + 본문 제작 (23개 파일)
"이 건물 대출 몇 % 나오나요?"를 주제로 박창현 팀장의 현장 상담·거래 경험을 담은 인스타/스레드용 카드뉴스 한 세트를 새로 만들었습니다. `cards.json → cards-revised.json → cards-final.json`으로 초안·검토본·확정본 3단계를 거쳐, cover·number·body·comparison·checklist·cta로 이어지는 9장 카드를 5색 팔레트로 구성하고 `_html/card-01~09.html`을 거쳐 `card-01~09.png`(1080×1350)로 렌더링했습니다. `본문.md`에는 스레드 9토막, 인스타 캡션, SEO 키워드, 그리고 "80~90%·금리 4%대" 등 수치가 현장 체감치임을 못박는 데이터 출처·한계 주석까지 함께 작성했습니다. 핵심 메시지는 "대출은 비율이 아니라 금리·상환능력의 문제이며, 막혀도 은행마다 기준이 달라 끝까지 풀면 길이 있다"입니다.

## Building scope — jitda 신축개요 엔진 도로·일조 로직 보강 + 블로그 3편 (22개 파일)
jitda/BuildScope MVP의 사전 규모검토 정확도를 끌어올리는 작업이 집중됐습니다. `lib/road-setback.js`에서 건축법 §46 도로후퇴 계산과 §86② '너비 20m 이상 도로 접면 시 정북일조 배제' 판정(`maxFrontageRoadWidthM`)을 정비하고, 지적 측정 최협폭과 LURIS 도시계획 도로등급(광로/대로/중로)이 충돌할 때 보도·완충녹지 의심으로 강등하는 `gradeConflict` 신호를 추가했습니다. `verify-multiparcel-sunlight.mjs` 등 검증 스크립트로 다중필지 합필(`merge.js`) 시에도 광로 접면 정북일조 배제가 단일필지와 동일하게 적용되는지(이전엔 누락) 확인했고, `lib/login-modal.js`에는 3채널 OAuth 로그인 모달과 비로그인 이탈을 막는 결과 게이트 복구 오버레이·가입 환영 토스트를 붙였습니다. 함께 블로그 글 3편(용적률·건폐율 정리 / 689㎡ 근생 신축 규모검토 실사례 / 정북일조사선과 광로 예외)을 작성해 콘텐츠도 채웠습니다.

## wonbuilding AI TF — AI 가치평가 특허(ADP6124) 변리사 보충자료 + ERP 보고서 (19개 파일)
특허 'WONBUILDING AI 가치평가 모델'(ADP6124)의 변리사 보충질의에 대한 답변 패키지 Q1~Q10을 .docx로 정리하고, 핵심 항목(Q1 ML 가중치 결합수식, Q3 피드백 학습루프, Q4 신뢰도 산출, Q8 추가 실시예)은 .md 원본도 함께 갱신했습니다. 특히 변리사 회신 메일에서 [실시예 1]의 임대료 라벨(월 3,710만원)과 수익환원 산출값(8.1억)이 한 단락 안에서 어긋난 오류를 진단해, 월 1,200만원·연 NOI 1.44억·통합추정가 27.76억으로 3평가법이 수렴하는 제3안으로 확정하고 청구항 8·15의 표기 오류까지 정정 요청했습니다. 더불어 사장님 대면 보고용으로 ERP '목표' 모듈(OKR형 직원평가 발전방향) 1장 요약 HTML을 `_build_erp_1pager.py`로 빌드해, 화면 캡처를 base64로 인라인한 모바일 단독 실행 자료로 만들었습니다.

## dungeon writer — '던전 기록국' 경쟁/시장 분석 문서 작성 (4개 파일)
프리미엄 패키지 게임 '던전 기록국'의 시장·경쟁작 분석 문서 `COMPETITIVE_ANALYSIS.md`를 새로 작성했습니다. Papers Please(500만 장)·Golden Idol(50만~100만, 98% 긍정)·Roottrees 등 직계 비교작 성적과, 우리 '기록 처리' 메커닉과 사실상 동일한 한국 선례 Somi의 Legal Dungeon(78%)을 정리하고, 그 부진 원인("서류 처리가 노동으로 미끄러짐")에서 도출한 §4 설계 가드레일 5개를 못박았습니다. 이 분석을 `HANDOFF.md`의 'Recent decisions'에 표준 가드레일로 반영했고(기록 퍼즐은 짧고 가시적 결과 우선·'능동적 변형'을 유일 차별 훅으로 유지), `title.gd`에는 특정 씬으로 바로 진입하는 개발용 `--scene=` 인자를 추가해 챕터 2 리뷰 동선을 단축했습니다.

## (루트 파일) — Godot 엔진 아카이브 + 작업로그 런처 (2개 파일)
프로젝트 루트에 dungeon writer가 사용하는 Godot 4.6.3 엔진 관련 `godot.zip`과, 이번 데일리 작업로그 자동화를 구동하는 `worklog_launch.sh` 런처 스크립트가 변경되어 있습니다. 두 파일 모두 특정 프로젝트에 속하지 않는 공용 지원 파일입니다.
