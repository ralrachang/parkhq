# 데일리 작업로그 — 2026-07-17 (금)

> 전날(2026-07-17) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 2개 프로젝트, 12개 파일 변경.

## howmuch.go — 서버 안정화·영속화(OOM 격리 + Fly volume) 설계와 구현 (8개 파일)
7/16 첫 실사용에서 드러난 OOM 재부팅·산출물 소실 문제의 근본 대응으로, 서버 안정화·영속화 스펙(`2026-07-18-serve-resilience-design.md`)과 태스크별 구현 플랜을 작성하고 serve 계층 구현에 들어갔습니다. 핵심은 세 가지 — 분석 잡을 `pipeline.run` CLI 서브프로세스로 격리해 힙 누적과 OOM을 잡 1건 실패로 강등시키고, Fly volume을 부팅 시 `data`/`cache` 심링크로 배선해 온디맨드 산출물이 재시작·재배포에도 살아남게 하며, 잡 진행 중 auto_stop을 막는 keepalive와 SIGINT 드레인, 대기열 위치(`queued_ahead`) 표시를 더했습니다. 이에 맞춰 `fly.toml`에 볼륨 마운트·`HOWMUCH_VOL` 환경변수·`kill_timeout = "5m"`을 추가했고, 신규 테스트 3파일(`test_serve_jobs.py`·`test_serve_volume.py`·`test_run_save_json.py`)로 서브프로세스 스트리밍, 볼륨 시드·manifest 병합·멱등성, 그리고 잡이 도중에 죽어도 절단 JSON이 남지 않도록 `_save_json`을 tmp+`os.replace` 원자 교체로 만든 회귀 가드까지 갖췄습니다. 스펙에는 7/16 OOM의 원인 진단 정정(동시 실행이 아니라 장수 프로세스의 힙 누적)도 기록해 두었습니다.

## diwolbu web — 7/18자 뉴스 3건·꿀팁 1건 발행 콘텐츠 작성 (4개 파일)
다음 날(7/18) 발행분 에디토리얼 콘텐츠 4건을 JSON으로 작성했습니다. 뉴스는 3건 — 구글 제미나이 3.5 프로 출시 재연기와 알파벳 주가 하락, 네이버 AI 브리핑의 7/21 광고 도입(AI 답변과 광고 구분 안내), 넷플릭스 2분기 실적과 3분기 가이던스발 주가 급락('참교육' 흥행 포함) — 으로, 각각 구독자 관점의 실천 포인트와 출처 링크를 담았습니다. 꿀팁은 '문서 AI 중복 비교' 1건으로, 챗GPT·노션 AI·감마·코파일럿처럼 뿌리가 같은 '글 쓰는 AI' 구독이 겹칠 때 범용 AI 하나를 기준으로 진단·다운그레이드·해지하는 절차를 단계별로 정리했습니다.
