# 데일리 작업로그 — 2026-08-21 (금)

> 전날(2026-08-21) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 2개 프로젝트, 61개 파일 변경.

## senior youtube — 역사 쇼츠 '야누시 코르차크' 편을 대본부터 최종 렌더까지 한 바퀴 돌림 (49개 파일)
`factory` 파이프라인으로 `20260822_korczak` 잡 하나를 통째로 제작했습니다. `inbox/20260822_korczak.md`의 내레이션 대본을 13개 비트로 쪼개 `job.json`을 만들고, 단일 패스 TTS로 `narration.wav`를 합성한 뒤 실측 타임코드에 맞춰 12개 샷(0.15~64.2초)으로 재구성했습니다. 샷마다 시대·지역을 못 박은 영어 영상 프롬프트를 `prompts.md`·`prompts_authored.json`에 작성했는데, 다큐멘터리 톤 통일과 함께 텍스트·군복·정치 상징·왜곡된 손 같은 항목을 NEGATIVE로 일괄 차단하는 방식을 썼습니다. 이후 placeholder PNG 12장 → `clips_inbox` 실제 클립 12개 → `segments` 렌더 → `.srt`/`.ass` 자막 → concat까지 이어져 1080x1920 · 63.6초 결과물이 나왔고, `review.txt`에 제목·설명·태그·해시태그를 담은 검수 카드가 생성되어 업로드 승인 대기 상태(`status: rendered`)로 마무리됐습니다. `config/channel.json`에는 자막이 쪼개지면 안 되는 고유명사(야누시 코르차크, 트레블링카행 열차, 유대인 격리 구역, 아동 인권 등)를 `keep_together`에 추가했습니다.

## Auto IM2 — 어바웃플라워 IM 브랜드북 개정판 재작성 및 PDF 재출력 (12개 파일)
강남 신사동 재건축부지 물건(어바웃플라워)의 IM을 매매가 조정 기준으로 개정했습니다. 조정된 가격을 기준으로 토지·연면적 평당가와 만실 가정 수익률을 다시 산정해 `02_classified.json`·`04_draft.json`에 반영하고, 사용자 요청이던 '토지 평당 단가 메리트' 메시지를 표지·매수논지·핵심지표·가격메리트·투자포인트·종합 등 6개 슬라이드에 관통시켰습니다. 가격 조정 사실은 취소선 배지와 태그 형태로 5개 슬라이드에 일관되게 표기했고, 13페이지 구성 자체는 유지했습니다. 직전 버전(HTML·PDF·JSON)은 별도 백업 폴더로 옮겨 두고 브랜드북 HTML → 이미지 base64 임베드 HTML → 16:9 13페이지 PDF까지 다시 뽑았으며, 레이아웃 검증기에서 잡힌 푸터 충돌 1건은 스크린샷 대조 결과 오탐으로 확인해 최종 리포트(`run_summary.md`)에 근거와 함께 남겼습니다.
