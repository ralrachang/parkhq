# 데일리 작업로그 — 2026-08-05 (수)

> 전날(2026-08-05) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 2개 프로젝트, 439개 파일 변경.

## auto capcut — 하이퍼프레임 카드 전환 모션을 설계부터 4K 재렌더까지 한 사이클 완주 (264개 파일)

영상 카드(하이퍼프레임)의 애니메이션을 다듬는 작업을 설계 문서(`2026-08-05-hyperframe-motion-polish-design.md`)와 태스크 단위 구현 계획(`2026-08-05-hyperframe-motion-polish.md`) 두 건으로 먼저 못 박고, TDD로 순서대로 구현했습니다. 핵심은 세 가지입니다 — 아직 안 켜진 자막 줄을 흐리게 두던 `dim` 토큰을 걷어내고 `opacity:0`으로 완전히 감춰 "다음 줄이 미리 보이는" 스포일러를 없앴고, 카드가 통째로 교체될 때 본문 사각형만 위로 밀리며 바뀌는 `push_to` 전환을 `blend.py`에 새로 넣었으며, 풀샷↔카드 경계의 하드컷을 ffmpeg `alphamerge` 기반 페이드로 바꿨습니다. 전환 종류마다 이징을 갈라 줄 등장은 대칭 곡선(`smoothstep`), 카드 교체는 `ease_out_cubic`을 쓰도록 `transition_alphas`에 이징 주입을 열었고, `hyper_states`가 상태마다 카드 id를 실어 보내게 해서 "같은 카드 안 줄 켜짐"과 "카드 교체"를 구분할 수 있게 했습니다. 설계 단계에서 추정으로 남겨둔 값들은 실제로 측정해 반영했는데, 경계 페이드 램프를 구간 바깥에 두면 카드가 아니라 검정 필러와 섞여 블랙 플래시가 난다는 걸 4K 실측으로 잡아내고 램프를 구간 안쪽으로 옮긴 정정 기록이 설계 문서에 그대로 남아 있습니다. 실촬영 프로젝트(`26.07.24 테스트촬영`)를 통째로 재렌더해 카드 프레임 PNG, 전환 중간 프레임, 썸네일, `final.mp4`와 숏폼 2편까지 산출물 대부분이 새로 생성됐습니다.

## Auto IM2 — 빌딩 IM 2건을 브랜드북 스타일로 제작하고 다단계 검수 루프로 사실관계 오류를 걸러냄 (175개 파일)

빌딩 매매 IM(Information Memorandum) 자동 생성 파이프라인으로 두 물건(동양빌딩·와이엠빌딩)의 자료 PDF를 받아 데이터 브리프 → HTML 초안 → PDF까지 한 번에 돌렸습니다. 두 건 모두 BRAND BOOK 스타일(Archivo + Bebas Neue + Pretendard, 아이보리·잉크·보르도 팔레트)로 각각 14p/15p 구성을 잡았고, 물건마다 강점을 앞세운 특수 슬라이드를 따로 설계했습니다. 이번 작업의 무게는 디자인보다 **검수 루프**에 실려 있습니다 — `verify_layout.py` 자동 레이아웃 검증, 원본 raw JSON 대조 프로그램 검증, 독립 critic 에이전트 검수, 지적사항 반영 후 재검증, PDF 렌더 확인까지 5단계를 돌렸고, 그 과정에서 억 단위 환산이 10배로 틀어진 치명적 계산 오류, 평당가 산식 설명 오류, 원본에 없는 역세권 서술("급행 라인"), 정비사업 진행 단계를 실제보다 앞당겨 쓴 서술, 주차 방식 캡션 오귀속 등을 잡아 고쳤습니다. 시점에 의존하는 표현("신축 4년차", "잔여 약 9년")을 전부 준공일·만료일 기준 표기로 바꾸고 자료 기준시점을 명기해, 시간이 지나도 문서가 거짓말을 하지 않도록 정리했습니다. 공부상 용도와 실사용이 다른 층에 대한 리스크 고지도 새로 추가했고, IM에 담지 않되 영업 시 매도자에게 확인해야 할 항목은 `run_summary.md`에 따로 남겼습니다. 슬라이드 육안 검증용 QA/리뷰 스크린샷이 물건당 30여 장씩 쌓여 변경 파일 수의 대부분을 차지합니다.
