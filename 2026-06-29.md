# 데일리 작업로그 — 2026-06-29 (월)

> 전날(2026-06-29) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 4개 프로젝트, 76개 파일 변경.

## building sns — 장마철 건물 관리 카드뉴스(BUILPAGO) 한 세트 제작 (47개 파일)
BUILPAGO 부동산 리포트용 인스타/스레드 카드뉴스 한 세트를 새로 만들었습니다. 주제는 "장마철 건물 관리, 누수와의 싸움"(`2026-06-29-jangma`)으로, 표지부터 CTA까지 총 10장을 navy·blue·brown·green·purple 5색 팔레트와 골드 하이라이트(#F2D11A)로 구성했습니다. 옥상 방수층·배수로·테라스·외벽 크랙 점검 체크리스트, 건물주와 임차인의 공용부 관리 책임 구분, 누수 흔적이 매각에 미치는 영향 등을 원빌딩부동산중개 박창현 팀장 화자로 풀어냈고, `cards-final.json`을 기준으로 generator.js를 돌려 인스타용 4:5 PNG(`card-01~10.png`)와 카카오톡 소식용 가운데정렬 안전본(`center/card-XX.png`)을 함께 뽑았습니다. 변경 파일 대부분은 이 두 버전의 `_html` 중간 산출물과 PNG, 그리고 `cards.json`/`cards-final.json`입니다.

## diwolbu — 디월부 앱 스토어 등록용 비주얼 자산 후보 제작 (11개 파일)
디월부(AI 구독 = 디지털 월세 가계부) 앱의 스토어 등록 이미지를 후보 형태로 정리했습니다. 구글 플레이 피처 그래픽(`feature-graphic-1024x500.png`)과 A/B 변형, 512px 스토어 아이콘 본본 및 A/B 후보, 앱 아이콘 후보(`icon-candidate-A/B`), 아이콘 네임 타이포 시안(`icon-name-1~3`)까지 모두 `assets/store/`에 묶었습니다. 최종안을 고르기 위한 아이콘·피처 그래픽 시안 비교용 묶음으로 보입니다.

## diwolbu web — 디월부 웹사이트 초기 셸과 공통 레이아웃 컴포넌트 구축 (11개 파일)
디월부 웹사이트를 Next.js 15 / React 19 / TypeScript 기반으로 새로 띄우고 공통 레이아웃의 뼈대를 잡았습니다. 한국어 메타데이터("디월부 — AI 구독도 월세입니다")를 담은 `app/layout.tsx`와 `globals.css`, `app/icon.svg`를 두고, 상단바(`TopBar` — 디월부 로고 + 로그인·내 구독·구독비 계산하기 버튼), 활성 항목을 청록(#0E9C8E)으로 강조하는 가로 메뉴(`MenuNav`), 푸터(`Footer`) 등 메인·서브페이지가 공유할 컴포넌트를 만들었습니다. `next.config.mjs`·`tsconfig.json`·`package.json`/`package-lock.json` 등 프로젝트 설정 파일도 함께 추가됐습니다.

## Building scope — OAuth 다중 로그인 확장과 도면 렌더 단일출처화 (7개 파일)
Jitda·BuildScope(사전 규모검토 도구)에서 로그인과 도면 렌더 양쪽을 손봤습니다. 로그인은 기존 카카오 전용에서 카카오·네이버·구글 3종으로 확장했는데, `lib/auth-providers.js`에 provider별 엔드포인트와 유저 파싱을 추상화하고 `routes/auth.js`가 이를 받아 `/auth/{provider}` 흐름을 일괄 생성하도록 했으며, 마이그레이션 없이 기존 `kakao_id` 컬럼을 `naver:`/`google:` 프리픽스로 재사용했습니다. 더불어 랜딩에서 심은 `jitda_acq` 첫터치 쿠키를 첫 로그인 시 `acq_source/medium/campaign`으로 기록하되 손상·부재 시 `direct`로 떨어뜨려 거짓 채널을 만들지 않고, 재로그인 시 첫 유입원을 보존하는 ICP 유입경로 추적을 붙였습니다. `routes/render-svg.js`는 `resolveParcelScope` 공유 헬퍼로 배치도·단면도·평면도·3D 매스를 같은 수치로 그리는 단일 출처(화면≡PDF≡도면)를 정리하고 다중 필지 합산 렌더까지 다뤘으며, 이 흐름을 실제 OAuth 콜백 경로로 검증하는 E2E 스크립트(`verify-icp-oauth-acq.mjs`)와 다중필지 렌더 검증 스크립트(`verify-multiparcel-render.mjs`)를 추가했습니다.
