# 데일리 작업로그 — 2026-06-22 (월)

> 전날(2026-06-22) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 6개 프로젝트, 814개 파일 변경.

## Auto IM2 — 김현우·박정규 대표님 '주간 추천물건 6선' 다이제스트 2건 제작 (662개 파일)
`weekly-digest` 스킬로 김현우·박정규 두 대표님 각각에게 브리핑할 **추천물건 6선 다이제스트**를 제작했습니다. `_brief.md`를 데이터 원천으로 세븐일레븐(숭의여대점)·불타는우동·호반·도화동 씨유·역촌동 소풍가는날·생각나는데 6개 매물(매매가 합계 96.5억, 수익률 3.12~4.04%)을 정리하고, 물건당 3p였던 구조를 **개요→위치·지역(신규)→강점·임대→엑싯플랜 4p로 확장**해 총 27슬라이드로 재설계했습니다. brandbook/corporate 두 디자인 스타일로 16:9 HTML을 만들어 `[won]…pdf`로 출력했고, 변경 파일 대부분은 레이아웃 넘침 검증용 QA 캡처(`_qa_loc`의 kw/pj × brandbook/corporate × 슬라이드 s04·s12·s24, 타임라인 시트, PDF 페이지 대조 이미지)입니다.

## building sns — BUILPAGO '금리 4% 빌딩 매수' 카드뉴스 9장 생성 (92개 파일)
BUILPAGO 부동산 리포트 카드뉴스 **"금리 4%, 지금 빌딩 사도 될까 — 수익률 구조로 판단하라"**(`2026-06-19-buytiming`)를 마무리했습니다. `cards.json → cards-revised.json → cards-final.json` 검토 파이프라인을 거쳐 9장짜리 카드를 확정하고, generator.js로 인스타·스레드용 4:5(1080×1350)와 카카오톡 소식 카드뷰용 3:4(1080×1440) PNG를 동시에 렌더링했습니다. 본문은 "일반 근생 3~4% vs 운영형 숙박업 10~15%", "법인 RTI 1.2~1.3 실제 적용", "공포에 사라" 등 12년차 중개인 관점의 스레드·인스타 캡션으로 작성했습니다. 이와 함께 26.06.22자 카카오톡 상담 캡처 자료가 input에 새로 추가됐고 CLAUDE.md 작업 지침도 갱신됐습니다.

## diwolbu — 구독 해지 도우미·요금제 낮추기 'Wedge' 기능 신규 구현 (33개 파일)
구독 가계부 앱에 **결제 전 개입 + 한 탭 해지**를 핵심으로 하는 해지 도우미 기능을 새로 붙였습니다. 마이그레이션 `00005_cancellation_guides.sql`로 `subscription_services`에 `cancel_url·cancel_steps·cancel_difficulty` 컬럼을 추가하고 넷플릭스·유튜브 프리미엄·멜론·스포티파이·디즈니+·티빙·쿠팡 로켓와우·ChatGPT Plus·Claude Pro 등 주요 한국 서비스의 단계별 해지 가이드를 시드했으며, 값이 비어도 website_url/검색으로 폴백하도록 설계했습니다. 화면단에서는 해지 가이드 화면(`app/cancel/[id].tsx`), 해지 대신 더 싼 요금제로 갈아타는 다운사이즈 화면(`app/downsize/[id].tsx`), 추천 리스트 컴포넌트(`CancelSuggestionList.tsx`)를 추가하고, 결제 3일·1일 전 "결제 전 점검 — 계속 쓸까요?" 푸시 알림 로직(`notifications/schedule.ts`)을 구현했습니다. 함께 Warm Ledger 디자인 토큰·타이포·카테고리 파이차트 등 화면 폴리시도 손봤습니다.

## Building scope — 지구단위 건축한계선 후퇴 '화면=도면=PDF' 정합 버그픽스 (20개 파일)
Jitda·BuildScope 사전 규모검토에서 **지구단위 건축한계선 후퇴(dpSetbackM)**가 PDF(render.js)에만 반영되고 화면(server.js)·배치도(render-svg.js)·입력 모달에는 빠져 화면 규모가 PDF보다 과대 표시되던 문제를 잡았습니다. 공유 헬퍼 `applyDistrictSetback`로 화면·도면·PDF 3채널의 유효 대지 차감을 일치시키고, "모름(ack)" 입력 시 거짓 후퇴를 만들지 않도록 회귀 0을 보장했습니다. 검증 스크립트 `verify-district-setback*.mjs`로 단위 산식·SPA 모달·`/api/analyze`·SVG 배치도까지 E2E로 확인했고, 양평 진단용 스크립트와 도면 좌표 변환(`render/geometry.js`)·인증(auth.js·jwt.js)·면적 산정(calculate.js·merge.js) 관련 파일도 함께 정리했습니다.

## worldcup dashboard — 2026 월드컵 조별리그 2차전 시드 데이터 갱신 (5개 파일)
2026 월드컵 대시보드의 서버 시드 데이터(`fixtures·groups·results·highlights·source-games`)를 최신 경기 결과로 업데이트했습니다. 48개국 12개 조(A~L) 구성을 유지한 채 조별리그 2차전 결과(아르헨티나 2-0 오스트리아, 뉴질랜드 1-3 이집트, 우루과이 2-2 카보베르데 등)와 스코어·헤드라인·경기장 정보를 반영했고, 각 경기·하이라이트의 유튜브 검색 링크와 출처(네이버 스포츠)를 함께 채웠습니다.

## wonbuilding AI TF — AI 구독 비용 지원 지급정산 내역 정리 (2개 파일)
AI 비용 지원 지급결재용으로 AI 구독 결제내역 엑셀을 정리했습니다. 2026년 5~6월 지급정산 시트(`AI구독_결제내역_2026-05~06_지급정산.xlsx`)와 6월 결제내역(`AI구독_결제내역_2026-06.xlsx`) 두 파일을 갱신했습니다.
