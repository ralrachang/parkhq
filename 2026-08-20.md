# 데일리 작업로그 — 2026-08-20 (목)

> 전날(2026-08-20) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 3개 프로젝트, 80개 파일 변경.

## senior youtube — 세계사 쇼츠 3편 완성과 제작 파이프라인 결함 4건 수정 (59개 파일)
정통 세계사 쇼츠 채널의 `factory` 파이프라인을 돌려, 2편 「전쟁의 승패를 가른 유리병 하나」(나폴레옹 군대와 니콜라 아페르의 병조림)를 자막 싱크와 음량을 고쳐 다시 렌더했고, 3편 「페르시아가 이집트를 무너뜨린 무기는 고양이였습니다」(펠루시움 전투)를 10샷 63.8초 분량으로 새로 완성해 검수 대기 상태로 올렸습니다. 완성본이 유튜브 기준보다 8dB 작게 나오던 원인이 `render.py`의 `amix`가 입력 개수만큼 볼륨을 나누는 데 있다는 것을 실측으로 확인해 `normalize=0`과 리미터를 넣고, `config/channel.json`의 나레이션·BGM 기준 라우드니스를 함께 재조정했습니다. 3편을 제작하며 드러난 나머지 결함도 같이 고쳤습니다 — `tts/base.py`의 문장 경계 스냅이 끝 여백에 붙어 마지막 자막이 0.1초로 찌그러지던 문제, `script_parser.py`가 긴 문장을 쉼표에서 자를 때 공백을 먹어 단어가 붙어버리던 문제, `subtitles.py`에서 "단 한 / 발의 화살도"처럼 수관형사가 자막 조각 끝에 홀로 남던 문제이며, 인명·지명이 갈라지지 않도록 `keep_together` 목록도 보강했습니다. 남은 과제인 지도 샷의 가짜 지명(AI가 만들어내는 존재하지 않는 지명)은 후처리 오버레이를 만들기 전까지 계속 발생한다는 점을 `진행상황.md`에 근거와 함께 기록해 뒀습니다.

## diwolbu web — 8월 21일자 데일리 콘텐츠 발행 + 구독 카탈로그 요금 정정 (17개 파일)
구독 정보 사이트의 하루치 콘텐츠를 발행했습니다. 뉴스 4건(LeVo 2 무료 AI 음악과 수노 비교, 챗GPT의 애플 메시지 플러그인, 유튜브의 크리에이터 독점 업로드 제안, 피콕 요금 인상), 꿀팁 「한 달만 무료 플랜으로 내려가 보기」, 칼럼 「값은 그대로였습니다, 받은 것도 그대로였습니까」를 새로 썼고, 통신사 간편결제 제휴 프로모션 3건을 추가하면서 기존 프로모션 2건은 공식 원문을 재확인해 종료일 근거와 확인 일자만 갱신했습니다. 같은 작업에서 카탈로그 정정도 진행해, `lib/services.ts`·`lib/plans.ts`·`lib/details/canva.ts`의 캔바 대표 요금이 공식 한국 요금 페이지보다 40%가량 과대 표기돼 있던 것을 공식 표시가 기준으로 바로잡고 팀 요금제의 달러 표기를 원화로 교체했으며, 해지 경로와 무료 전환 시 워터마크가 붙는다는 사실도 추가했습니다. `lib/details/gamepass.ts`에는 갱신 결제 후 30일 안에 취소하면 최근 갱신 요금을 환불받을 수 있다는 공식 FAQ 내용이 빠져 있어(독자가 손해 보는 방향의 누락이었습니다) 이를 보강했습니다. 요금 정정 때문에 꿀팁 글의 절약액 합계가 어긋나 발행 편집을 다시 돌려 재계산했고, 타입 검사·단위 테스트 48건·빌드를 모두 통과시켰습니다.

## naver land — 좌표 지터 때문에 생기던 가짜 '정밀단독·새 주소' 판정 수정 (4개 파일)
네이버 부동산이 같은 건물 광고에도 소수점 끝자리가 미세하게(≈0.1~0.2m) 다른 좌표를 내보내기 시작하면서, 이날 오후 수집분부터 하루 150건 넘게 문제가 나타났습니다. 좌표를 문자열 완전일치로 묶던 기존 로직이 이를 "광고가 하나뿐인 처음 보는 좌표"로 오인해, 실제로는 같은 건물에 광고 이력이 여러 개 쌓인 매물까지 💎정밀단독·🆕새 주소로 피드 상단에 올리고 있었습니다. `app/store.py`에 격자 + union-find 방식의 건물 키 정규화(`building_keys`, 임계값 `COORD_EPS` 1.1m)를 넣어 근접 좌표를 한 건물로 묶고, 단독 판정과 새 주소 판정을 모두 좌표가 아닌 건물 단위로 수행하도록 바꿨습니다. 프로덕션 DB 사본으로 검증해 가짜 정밀단독 150건이 걸러지고 정상적인 새 주소 손실은 0건임을 확인했으며, 지터 회귀 테스트 7개를 새로 추가해 총 58개 테스트를 통과시키고 `config.toml`·`README.md`에 판정 기준과 임계값을 그렇게 잡은 근거를 함께 적어 뒀습니다.
