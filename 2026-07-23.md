# 데일리 작업로그 — 2026-07-23 (목)

> 전날(2026-07-23) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 6개 프로젝트, 102개 파일 변경.

## building sns — 성수동 매각 사례 SOLD OUT 카드뉴스 10장 제작 (48개 파일)
화·목 SOLD OUT 라인으로 성수동 뚝섬역 인근 3층 건물의 매각 사례를 다뤘습니다. 직접 촬영한 현장 사진 3장(`input/soldout/26.07.23/`의 front·corner·alley)을 넣고, `cards.json` 초안 → content-reviewer 검토본(`cards-revised.json`) → `cards-final.json` 확정 순서로 원고를 굳힌 뒤 generator.js로 PNG를 뽑았습니다. 산출물은 인스타·스레드용 4:5 카드 10장과 카카오톡 소식용 가운데정렬 안전형(`center/`) 10장 두 벌이고, 중간 렌더 HTML(`_html/`)도 함께 남았습니다. 서사는 후킹(사진) → 물건 소개 → "만실인데 수익률이 낮다"는 의문 → 땅값 비중 해부 → 호가와 계약가의 간극 → 완성형 건물이라는 결론 → CTA로 이어지고, `본문.md`에는 스레드 토막 3개·인스타 캡션·SEO 키워드와 함께 "직접 중개한 거래가 아닌 공개 실거래가 기반 제3자 분석"이라는 화자 원칙과 데이터 출처·한계를 명시했습니다.

## auto capcut — 실거래가 차트 오버레이(Phase 3b)와 완성 패키지(Phase 4) 구현 (44개 파일)
영상 자동편집 파이프라인의 두 단계를 계획서부터 실제 코드까지 밀어붙인 날입니다. Phase 3b는 국토부 실거래가 API 클라이언트(`backend/data/molit.py`)와 차트 생성 단계(`backend/pipeline/charts.py`)를 새로 만들어, 분석 결과의 `chart_requests`를 월별 평균 시세 시계열로 조회하고 matplotlib 투명 PNG로 그린 뒤 assemble이 이를 `overlays`로 조립하고 renderer가 ffmpeg overlay+fade로 발화 시점에 합성하도록 연결했습니다. Phase 4는 선명도(라플라시안) 기준 베스트 프레임에 문구를 얹는 썸네일 3안(`thumbnail.py`), 인트로/아웃트로 타이틀카드 생성과 concat 결합, 9:16 숏폼 렌더까지 묶어 "한 번 실행 = 롱폼 + 숏폼 + 썸네일" 패키지를 목표로 잡았습니다. 두 계획 모두 실패 테스트 → 구현 → 통과 순의 TDD 절차와 "차트·썸네일 단계는 실패해도 예외를 던지지 않고 산출물 없이 건너뛴다"는 계약을 못박았고, 실제로 `test_clip` 프로젝트에서 charts.json과 차트 PNG가 생성되고 final.mp4까지 렌더되는 것을 확인했습니다.

## team ERP — Plaud 자동적재 배관을 Zapier에서 VPS+n8n으로 전환 착수 (5개 파일)
Plaud Note 녹음이 ERP 수신함(`plaud_inbox`)까지 자동으로 흘러가는 배관에서 Zapier가 맡던 "트리거 수신 + HTTP 중계" 역할을 자가호스팅 n8n으로 옮기는 설계서와 10개 태스크 실행계획을 작성했습니다. 핵심 원칙은 변경 범위 최소화로, 기존 Edge Function `plaud-ingest`와 규칙기반 분류 `classify.ts`, DB 스키마, `x-plaud-secret` 인증 계약은 전부 손대지 않고 n8n에는 트리거 검증과 오디오 영구저장 두 역할만 맡깁니다. 함께 그동안 저장하지 않던 녹음 파일을 보관하기 위해 마이그레이션 `0010_plaud_audio_storage.sql`로 비공개 Storage 버킷과 열람 역할을 제한하는 RLS 정책을 추가했고, 인프라는 n8n + PostgreSQL 도커 컴포즈로 구성했습니다. 리버스 프록시는 설계 단계의 Caddy 대신, 이미 다른 사이트를 서빙 중인 VPS의 기존 nginx 컨벤션을 따르도록 실제 구성 파일에서 방향을 바꿨습니다.

## diwolbu web — 하나카드 구독 프로모션 콘텐츠 2건 발행 (2개 파일)
`content/promos/`에 7월 23일자 카드사 구독 프로모션 2건을 JSON으로 정리했습니다. 하나는 Claude·Perplexity 정기구독 결제액을 하나머니로 돌려주는 건(7월 말 종료), 다른 하나는 쿠팡 와우·네이버플러스·넷플릭스·유튜브 프리미엄 월간 이용권 대상 건(8월 말 종료)입니다. 두 건 모두 혜택 요약뿐 아니라 실제로 걸려 넘어지기 쉬운 조건 — 공식 PC 홈페이지 결제만 인정되고 앱마켓·간편결제는 제외된다는 점, 최근 결제 이력이 있는 서비스는 대상에서 빠진다는 점, 적립 시점과 조기 종료 가능성 — 까지 풀어 썼고 카드사 공식 이벤트 페이지를 출처로 달았습니다.

## sise-web — 건물 소유주 우편 발송 워크플로우 사전 검토 (2개 파일)
시세 분석 서비스의 다음 트랙 후보로 "특정 법정동 근생 건물 소유주에게 우편을 보내는 워크플로우"를 조사하고 검토 노트를 남겼습니다. 결론은 전면 자동화가 불가능하다는 것으로, 현재 쓰는 건축HUB 표제부 API에는 소유자 필드가 아예 없고 소유자현황은 정부24·세움터에서 건별 간편인증으로 발급받는 건축물대장에만 있는데 이 구간의 로그인 자동화와 대량 수집은 ToS 위반 소지와 개인정보보호법 리스크 때문에 만들지 않기로 정했습니다. 대신 5단계를 자동(대상 주소 목록 추출) / 수동(대장 개별 발급) / 자동(PDF 일괄 파싱) / 반자동(우편 발송은 최종 확인 후 사람이 실행)으로 쪼갠 표를 합의해 두었고, `RESUME.md`의 다음 할 일 목록에도 "검토만, 미착수" 상태로 반영했습니다.

## wonbuilding AI TF — GCP 에이전트 구축 재견적서 갱신본 정리 (1개 파일)
AI 에이전트의 GCP 아키텍처 구축 건에 대한 외주 재견적서 PDF를 갱신본으로 교체해 보관했습니다. 사흘 전 판본과 비교하면 항목 구성이 바뀌어, "자동 분석·보고서 생성"(주변 임대시세 자동 분석과 브리핑 보고서 자동 작성)이 범위에서 빠지면서 기획·PM, 백엔드 에이전트 개발, 고객-매물 매칭 및 RAG, WCD API 연동, 인프라·QA·납품의 5개 항목으로 재구성됐습니다. 범위가 줄어든 만큼 총 개발공수와 기간, 그리고 할인 후 제안 금액도 함께 조정된 판본이며, 구체 금액·단가·할인 조건은 협상 진행 중이라 이 로그에는 옮기지 않습니다.
