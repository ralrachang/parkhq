# 데일리 작업로그 — 2026-07-06 (월)

> 전날(2026-07-06) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 8개 프로젝트, 438개 파일 변경.

## Auto IM2 — 빌딩 매물 3건의 IM(투자설명서) 제작·PDF 출력 완료 (213개 파일)
럭키프라자빌딩·솔약국(메디컬)·포샤네일 세 물건의 자료 PDF를 input에 투입하고, 파이프라인으로 물건별 데이터 브리프(`_brief.md`)를 정제한 뒤 브랜드북·코퍼레이트 2스타일 HTML을 생성했습니다. 세 건 모두 코퍼레이트(인베스트먼트 프로스펙터스) 스타일이 채택되어 최종 PDF로 출력했고(솔약국 13페이지, 포샤네일 6페이지), `verify_layout.py` 레이아웃 검증 CLEAN(0 이슈)과 2배 해상도 QA 스크린샷 육안 확인까지 마쳤습니다. 브리프에는 각 물건의 입지·건물 개요·임대현황·투자 포인트·엑싯플랜(HOLD/VALUE-UP/EXIT)과 리스크 솔직 고지를 담았으며, 견적·수익률 기준값은 만실 기준 명기 원칙으로 정리했습니다. 파일 대부분은 추출 이미지·QA 캡처·임베드 HTML 등 산출물입니다.

## howmuch.go — 매매·임대 추정 정확도 개선: 백테스트 A/B로 보정 로직 채택 (89개 파일)
"매매·임대 추정 정확도/신뢰도 개선" 구현 계획(`2026-07-06-estimate-accuracy.md`)을 세우고 SDD 방식(Task 4~13 브리프·리포트)으로 실행했습니다. 시점 보정 모듈 `pipeline/index_adjust.py`(Theil-Sen 자체 트렌드)를 신설하고 `analyze_sale`·`process_rental`에 개선 플래그 게이트를 넣었으며, unittest 테스트 6종을 함께 작성했습니다. 마지막 Task 13에서 백테스트 하네스로 138개 타깃 A/B를 돌려 시점 보정(time_adjust)·거리 감쇠(λ=200)·호가 β(auto)를 채택, 중앙 절대 오차율(MdAPE)을 27.3%에서 22.0%로 개선했고 deal_weight는 기존 기본값 유지로 판정했습니다. `cache/backtest/`에 후보 설정·결과 JSON들이, `data/`에 재생성된 매매·임대 리포트 HTML이 남아 있습니다.

## sise-web — 시세 웹서비스 신규 구축: Phase 0 검증 GO + 로컬 웹앱(Plan 2a) 완성 (89개 파일)
상업용 부동산 시세 웹서비스의 설계 스펙과 엔진 구현 계획(Plan 1)·로컬 웹앱 계획(Plan 2a)을 작성하고 하루에 완주했습니다. Phase 0에서 네이버 부동산 수집을 검증해 구 `new.land` 라우트가 폐기되고 `fin.land`(네이버페이 부동산)로 이전된 사실을 실측, WAF가 외부 요청을 차단하므로 Playwright in-page fetch 방식으로만 수집 가능하다는 조건부 GO 판정을 `phase0-verdict.md`에 기록했습니다. Plan 2a는 9개 태스크 전부 완료 — Express+SQLite job 큐, Python 상주 워커, 주소 검색 UI·Leaflet 대시보드·조회 이력 페이지를 만들고, 실제 주소로 job 생성→수집→임대·매매 추정→신뢰도 표시까지 E2E 라이브 검증 후 XSS 수정을 거쳐 GitHub에 push했습니다. 인증·멀티유저 등 2b 이월 항목은 `PLAN2-TODO.md`에 정리했습니다.

## diwolbu web — Supabase 실제 인증(카카오·구글·이메일) + 내 구독 서버 동기화 구현 (26개 파일)
데모 로그인을 실제 인증으로 교체하는 설계 스펙·구현 계획을 작성하고 TDD로 구현했습니다. 구독 스냅샷 합집합 병합 유틸(`lib/mysubs.ts`), 인증 오류 한국어 매핑(`lib/authErrors.ts`), Supabase 브라우저 클라이언트 싱글턴, `useAuth` 훅, 동기화 모듈(`mysubsSync.ts`, 로그인 시 병합 + 디바운스 업서트)을 만들고 vitest를 도입했습니다. LoginForm을 카카오·구글·이메일 인증으로 재작성하고 비밀번호 재설정 페이지(`/reset-password`)와 TopBar 로그인 상태 표시를 추가했으며, `my_subs` 테이블 마이그레이션 SQL과 비개발자용 Supabase 설정 가이드 문서도 작성했습니다. env 미설정 시 비로그인 모드로 완전 동작하도록 설계했고, 계산기의 사용 빈도(자주/가끔/거의 안 써요) 입력 기능도 이날 완료로 기록됐습니다.

## Building scope — 상가주택(주택+근생 혼합) 모델 설계·구현 (14개 파일)
다세대 주택층 4개층 캡 이후 남는 용적률을 하층 근생으로 채우는 상가주택 혼합 모델의 설계 문서(`sanga-mixed-model`)를 작성하고 엔진에 반영했습니다. `calculate.js`에 혼합 허용 게이트와 층 채움 규칙(하층 근생 + 상부 4개층 다세대)을, `parking.js`에 용도별 주차 합산(주택 세대 기준 + 근생 시설면적 기준, 법령 조항 병기)을 구현했고, 개요·평면도 v3 렌더러가 근생층·주택층을 자동 분리하도록 했습니다. 사업성 단가 미분리는 개략치임을 온스크린 카드로 정직하게 표기하고, `verify-sanga-mixed.mjs` 등 검증 스크립트 4종으로 회귀를 확인했습니다.

## team ERP — Plaud Note 녹음 자동적재 배관 구축 (3개 파일)
Plaud Note 녹음이 전사·요약되어 ERP 수신함에 자동으로 쌓이는 연동을 만들었습니다. 구조는 Plaud → Zapier(Webhook + 공유 시크릿) → Supabase Edge Function `plaud-ingest` → `plaud_inbox` 테이블이며, 수신함 테이블·RLS 마이그레이션(`0008_plaud_inbox.sql`)과 수신 엔드포인트(필드명 변형 흡수, 원본은 raw 컬럼 보관, 멱등 upsert)를 구현했습니다. 배포 절차·스모크 테스트·Zapier Zap 구성·기기 세팅 체크리스트까지 담은 운영 런북 문서도 작성했습니다. 다음 단계로 실제 payload 필드 확정과 ERP 수신함 UI 개발을 남겨뒀습니다.

## yangjae_NI — sise 엔진 기획의 출발점: 계획·스펙 문서와 네이버 API 샘플 확보 (3개 파일)
기존 동네별 검증 스크립트를 공용 패키지로 일반화하는 sise 엔진 구현 계획과 시세 웹서비스 설계 스펙이 이 저장소에도 기록됐고, 네이버 `fin.land`의 boundedArticles API 샘플 응답 JSON을 캡처해 저장했습니다. 이 샘플이 sise-web의 normalize 매핑(공급/전용면적, 원 단위 가격 등 스키마 분석)의 기초 자료가 됐습니다.

## idea — 나이키 커머스 디자인 시스템 분석 문서 작성 (1개 파일)
`DESIGN-nike.md` — 나이키 웹의 디자인 시스템을 토큰화해 정리한 분석 문서입니다. 컬러(블랙/화이트/소프트 그레이 중심 + 세일 레드 등 시맨틱 최소 사용), 타이포그래피(Futura ND 96px 캠페인 디스플레이 + Helvetica Now UI 계층), 8px 스페이싱, 필(pill) 버튼 지오메트리, 무그림자 플랫 카드 등의 원칙을 front-matter 토큰과 컴포넌트 스펙·Do/Don't·반응형 규칙으로 문서화했습니다.
