# 데일리 작업로그 — 2026-07-21 (화)

> 전날(2026-07-21) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 9개 프로젝트, 216개 파일 변경.

## Auto IM2 — '두런두런' 빌딩매매 IM(투자설명서) 신규 제작 (75개 파일)
빌딩매매 IM 자동 생성 시스템으로 서울 중구 신당동 코너 물건 '두런두런'의 매수 브리핑 자료를 새로 만들었습니다. 물건 PDF를 파싱해 `_brief.md`에 기본정보·토지·건물·임대차 현황과 3~5대 강점(초역세권 코너, 힙당동 상권 호재, 밸류업 여지 등), 리스크(구축·공실·주차)를 정리하고, 위치도·지적도·현장 사진을 크롭해 이미지로 매핑했습니다. 디자인은 원빌딩 명함톤(브라운·베이지) 팔레트의 브랜드북 스타일로 슬라이드를 구성했고, 레이아웃 넘침을 잡기 위한 QA 스크린샷 13장과 플래그 리포트(`_qa/*`)를 함께 산출했습니다. 매매가·수익률 등 구체 지표는 자료 내부용으로 관리했습니다.

## building sns — 장충동 매각 사례 SOLD OUT 카드뉴스 10장 (47개 파일)
화·목 매각 사례 리뷰(SOLD OUT) 라인으로, 동대입구역 앞 장충동 단층 물건의 공개 실거래 분석 카드뉴스를 제작했습니다. `cards-final.json`에 후킹→물건 소개→"건물값 0원"→대지 평당가→재건축 함정→강점·약점 비교→매수자 추정→계약 기간→결론 체크리스트→CTA로 이어지는 사건 전개형 서사 10장을 설계했습니다. 이를 HTML 10장으로 렌더한 뒤 인스타/스레드용 4:5 PNG와 카카오톡 소식용 가운데 정렬(center) PNG를 자동 생성했고, 원본 사진(corner·side)에는 차량 번호판 블러 마스크를 좌표로 지정해 처리했습니다.

## mer's blog — 투자 블로거 '메르' 글 코퍼스 수집·색인 (38개 파일)
네이버 블로거 '메르'(ranto28)의 글을 카테고리별(cat-17/19/21/28)로 수집해 검색 코퍼스를 갱신했습니다. 7월 21일자 신규 글로 삼성전자·SK하이닉스의 CXL(Compute Express Link) 신상 메모리 이야기 등을 마크다운으로 저장하고, `_manifest.json`을 업데이트한 뒤 BM25 검색 인덱스(`bm25.pkl`)를 재생성했습니다. 이 코퍼스는 `/mer` 투자 상담 스킬이 참고하는 지식 베이스로 쓰입니다.

## auto capcut — 영상 자동 편집 스튜디오 착수(설계+파이프라인 MVP) (30개 파일)
직접 촬영한 영상을 넣으면 무음 컷편집·자막·차트·BGM·썸네일·숏폼까지 자동 처리하는 '영상 자동 편집 스튜디오'를 새로 시작했습니다. 전체 설계 문서(프로젝트 JSON 중심 3분리 아키텍처, 8단계 파이프라인, GUI/렌더러 명세)와 Phase 1 구현 계획을 작성했고, 그 계획에 따라 백엔드 파이프라인 모듈(ingest·silence·stt·cues·bgm·assemble)과 렌더 모듈(ffmpeg_util·subtitles·timemap)을 TDD 방식으로 구현했습니다. 테스트 11종을 함께 붙여 "영상 1개 → 무음 잘리고 자막·BGM 입혀진 mp4"를 만드는 CLI MVP의 뼈대를 세웠습니다(faster-whisper CPU 기반).

## blog auto — 매각 사례 PDF → 네이버 블로그 글 2건 자동 생성 (20개 파일)
원빌딩 물건 브리핑 PDF를 분석해 네이버 블로그용 HTML 글을 만드는 파이프라인으로, '우리은행(금호도)'과 '장충동 우레카츠' 두 건을 생성했습니다. 각각 draft·post HTML 본문과 근거(reasons) 카드(HTML/PNG), CTA 이미지, 썸네일을 산출했습니다. '장충동 우레카츠'는 같은 소스 PDF를 building sns 카드뉴스와 공유해, 하나의 매물을 카드뉴스와 블로그 두 채널로 동시에 콘텐츠화한 형태입니다.

## wonbuilding AI TF — 베스핀글로벌 Google Workspace 견적 자료 취합 (3개 파일)
회사 AI TF 관련으로, 베스핀글로벌이 공유한 Google Workspace(GWS)+GE 도입 견적 자료를 받아 정리했습니다. 멀티플랜(PDL) 견적서 PDF와 퀵스타트 가이드를 압축본과 함께 폴더에 취합했습니다. 견적 기준값과 플랜 구성 등 구체 금액·계약 조건은 자료 원본으로만 관리했습니다.

## auto — 자막 테스트 파일 1건 (1개 파일)
auto capcut의 자막 생성 테스트(`test_subtitles.py`)에 해당하는 단일 파일 변경이 감지됐습니다. 경로 형태로 보아 auto capcut 자막 모듈 작업에서 파생된 미러/이동 파일로 보이며, 독립적인 별도 작업물은 아닙니다.

## Building scope — 스레드 글 포맷 가이드(기준선) 확정 (1개 파일)
Jitda·BuildScope의 스레드(Threads) 게시글 집필 기준 문서를 확정했습니다. 바이럴 패턴 리서치와 전날 글 대조를 근거로 "돈·손해 훅 + §조항 증거" 방향의 5단 템플릿을 세우고, JITDA(규제·팩트)와 BUILPAGO(현직 중개사 1인칭) 계정별 톤 분리, 정확도 100% 원칙과 연동한 정직성 가드레일, 게시 전 자가 검수 체크리스트, 실제 케이스 Before/After 예시까지 담았습니다. 이후 모든 스레드 글은 이 문서를 기준으로 집필·검수합니다.

## diwolbu web — 앤트로픽 '250억 청구서' 후속 뉴스 1건 (1개 파일)
디월부 뉴스 섹션에 앤트로픽 과다 청구 사건의 후속 기사를 추가했습니다. 청구 이메일뿐 아니라 실제 카드 결제 시도가 10여 차례 있었고 '자동충전 설정 오류'라는 회사 답변에 당사자가 반박했다는 새 사실을 정리했습니다. 이를 AI 에이전트 종량 과금 시대의 구조적 리스크로 짚고, 결제 수단·API 키 점검, 지출 한도 설정·자동충전 확인, 거액 청구 시 카드부터 정지 등 독자 대응 가이드와 출처를 JSON 콘텐츠로 작성했습니다.
