# 데일리 작업로그 — 2026-07-08 (수)

> 전날(2026-07-08) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 6개 프로젝트, 450개 파일 변경.

## Auto IM2 — 추천물건 3선 묶음 IM(다이제스트) 제작 (150개 파일)
빌딩매매 IM 자동 생성 시스템의 `weekly-digest` 스킬로, 한 고객(원장님) 브리핑용 "도심 코어 명도가능 3선" 다이제스트를 제작했습니다. 한남동·통의동·권농동 세 물건(목단가옥·미주구리·대륙빌딩)의 개요·강점·엑싯플랜을 하나로 묶고, 세 물건 공통점(주요 상권 메인 입지 + 매도인 명도 조건 가능)을 관통 메시지로 잡은 데이터·메시지 원천 `_brief.md`를 정리했습니다. input 폴더의 물건 PDF 3건을 추출해 brandbook·corporate 두 디자인 스타일의 12슬라이드 HTML을 생성하고, `_qa/` 레이아웃 넘침 자동 검증(FLAG 리포트)과 `_shots/` 슬라이드 스크린샷(s01~s12), 건물 사진 `img/` 인벤토리까지 파이프라인을 돌렸습니다. 매매가·수익률 등 구체 수치는 브리프에 사실대로 반영하되 명도 리스크 부재를 최대 셀링포인트로 프레이밍하는 원칙을 문서화했습니다.

## howmuch.go — 매매가 추정 백테스트 + 유사도 커널 튜닝 (142개 파일)
부동산 매매가 추정 엔진의 정확도를 실거래 leave-one-out 방식으로 측정하고 비교법 파라미터를 튜닝했습니다. `cache/_sweep_kernel.py`로 크기·건축연한·용도지역 불일치·거리감쇠(size_bw·age_bw·zone_miss·dist_decay_m) 후보 조합을 single/combo 배치로 스윕해 MdAPE·band_hit·mdwidth 지표를 표로 뽑았고, `cache/_regen.py`는 `sale.json`이 있는 등록 주소를 `--no-fetch`로 일괄 재생성하며 슬러그별 성공/실패를 수집했습니다. 강남·마포·성동 등 21개 주소 표본에 대한 백테스트 결과(baseline 전체 MdAPE 약 20.7%, band_hit 약 52.5%)를 `cache/backtest/`에 기록하고, 동별 실거래·건물대장·지오코딩 캐시와 지역별 매매 리포트(HTML)·지도 이미지를 다량 재생성했습니다.

## building sns — 빌딩 양도세 절세 카드뉴스 10장 제작 (90개 파일)
빌파고(BUILPAGO) SNS 카드뉴스 엔진으로 "양도세 타이밍" 주제 10장짜리 카드뉴스를 제작했습니다. cover→질문→보유기간 세율→장기보유특별공제→양도·증여·상속 통합 설계→지분 증여→이월과세→비교→체크리스트→CTA 순으로 흐름을 짜고, navy·blue·brown·green·purple 5색 팔레트를 카드마다 교차시켰습니다. cards.json 초안을 content-reviewer가 검토해 `cards-revised.json`으로 다듬은 뒤 `cards-final.json`으로 확정했고, generator.js로 인스타·스레드용 4:5 PNG와 카카오톡 소식용 가운데 정렬(center) PNG 두 버전을 각 10장씩 생성했습니다. 발행처·담당자 연락처와 DM 유도 문구를 담은 CTA 카드로 마무리했습니다.

## sise-web — 매매가 추정 정확도 개선 설계 + 실거래 캐시 수집 (61개 파일)
시세 분석 웹의 매매가 추정 정확도를 높이기 위한 구현 플랜·스펙 문서를 작성하고 근거 데이터를 수집했습니다. `2026-07-08-sale-price-accuracy` 플랜에서 실거래 비교를 주 앵커로 삼는 방향을 잡고, 가중 분위수/중앙값 유틸, 보정 상수 모듈(calib), 거리·시점·용도지역·규모 가중의 `estimate_comparable`, 수익환원 교차검증 verdict, 매매 신뢰도 등급, MOLIT 실거래 leave-one-out 백테스트 하네스까지 TDD 태스크 단위로 상세 설계했습니다. 이튿날 이어질 `adjacent-dong-comp`(인접 법정동 비교군 확장) 플랜·스펙도 함께 초안화했고, 종로구 일대 등 월별 MOLIT 실거래·건물대장 캐시(JSON)를 대량으로 내려받아 엔진 캐시를 채웠습니다.

## naver land — 매물 모니터 배포 구성 정비 (5개 파일)
네이버 부동산 매물 모니터의 실행·배포 뼈대를 정비했습니다. `run.py` 엔트리포인트는 수집→저장→사이트 생성→배포 파이프라인을 KST 로그와 함께 돌리고 `--dry-run`·`--rebuild-regions` 옵션을 제공하도록 구성했습니다. 배포 측은 python:3.12-slim 기반 Dockerfile(타임존 Asia/Seoul, GitHub Pages 배포용 git 포함, 서드파티 의존성 없이 stdlib)과 하루 2회(09:00/16:00 KST) 실행하는 crontab 예시를 정리했으며, app 패키지 초기화와 requirements 파일을 함께 손봤습니다.

## diwolbu web — 절약 뉴스 에디토리얼 2건 발행 (2개 파일)
디월부 웹의 `editorial-daily` 워크플로우(에디터 이절약)로 소비자 절약 관점 뉴스 2건을 발행했습니다. 첫 번째는 미국 기업들이 딥시크·GLM 등 중국 오픈소스 모델로 옮겨가며 벌어지는 AI 개발자 시장의 가격 전쟁을 다루고, 소비자 구독료에는 당장 영향이 없으니 상위 요금제 선결제를 서두르지 말라는 관점을 붙였습니다. 두 번째는 7월부터 네이버플러스 멤버십 경유 넷플릭스 업그레이드 요금이 인하돼 단독 구독보다 저렴해진 소식을 정리하고, 결합 할인의 이득과 함정(멤버십을 안 쓰면서 할인만 노리는 경우)을 짚었습니다. 각 기사에 요약·영향·본문·출처 링크를 갖춘 JSON 콘텐츠로 저장했습니다.
