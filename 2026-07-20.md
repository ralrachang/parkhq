# 데일리 작업로그 — 2026-07-20 (월)

> 전날(2026-07-20) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 6개 프로젝트, 702개 파일 변경.

## Auto IM2 — 7월 신규 추천물건 13선 주간 다이제스트 IM 제작 (585개 파일)
원빌딩 7월 신규 추천물건 PDF(07.01~07.19)를 입력으로 weekly-digest 파이프라인을 돌려, 13개 물건을 하나로 묶는 브랜드북 스타일 다이제스트 IM을 제작했습니다. 제작 브리핑 문서(`_brief.md`)에 표지·비교표·물건별 3페이지(개요/강점/엑싯플랜) 총 42슬라이드 구조와 물건별 데이터·강점·임대현황을 정리했고, Archivo·Bebas Neue·Pretendard 조합의 에디토리얼 매거진 무드로 방향을 확정했습니다. PDF 페이지맵·이미지 추출(`_pagemap.json`, `_index.json`, 프래그먼트 HTML), 히어로 이미지 후보 선정(`_hero_cands.json`)을 거쳤으며, 레이아웃 넘침 자동 검증 루프의 최종 리포트가 이슈 0건으로 통과하고 슬라이드 전장 스크린샷 QA(`_qa/shots/`)까지 완료했습니다. 585개 파일 대부분은 추출 이미지와 QA 스크린샷입니다.

## building sns — '계약 후 잔금 전 사고' 카드뉴스 10장 제작 (44개 파일)
빌파고 인스타그램용 카드뉴스 `2026-07-20-jangeum-sago`를 제작했습니다. "도장 찍었다고 끝난 게 아니에요"라는 후킹으로, 계약과 잔금 사이 2~3개월 구간에서 터지는 사고 4가지(대출 승인 한도 축소, 임차인 폐업·퇴거 변수, 잔금 전 등기부의 새 근저당·가압류, 매도인·매수인 변심)와 D-30/D-7/D-1 잔금 전 체크리스트를 10장 구성으로 풀었습니다. `cards.json` 초안 → 검토 반영 `cards-final.json` 확정 → generator.js로 HTML 10장과 인스타용 4:5 PNG 10장, 카카오톡 소식용 가운데 정렬 안전형(center) PNG 10장을 함께 생성했습니다.

## Building scope — 블로그·Threads 마케팅 자동화 구축과 실사례 콘텐츠 10편 (43개 파일)
BuildScope MVP에 마케팅 콘텐츠 파이프라인을 구축했습니다. 주소를 넣으면 엔진 산출값만 JSON으로 뽑는 `extract-facts.mjs`(글의 모든 수치는 이 출력에서만 인용하는 정직성 원칙)와 실사례 팩트 JSON 8건, 소재 큐 `queue.json`(14개 앵글)을 만들고, 블로그 대기열에 정북일조·용적률·도로폭 등 규모검토 실사례 글 10편을 적재했습니다. 이날 논현동 광로 접면 정북일조 면제 사례는 자동 집필로, 일반상업 용적률 사례는 수동 집필로 완료 처리했으며, 근거 없는 수치는 본문에서 제외하는 검증 노트를 함께 남겼습니다. Threads 자동 게시기 `post-threads.mjs`(토큰 자동 갱신·중복 게시 방지·게시 이력 jsonl), 발행 스크립트 `publish-post.mjs`, 사이트맵 빌드, 블로그 검증 스크립트 `verify-blog.mjs`와 blog/threads/render 라우트, calculate·landuse·vworld 등 엔진 라이브러리도 함께 정비했습니다.

## blog auto — 매각 사례 PDF→네이버 블로그 글 자동 생성 스킬 신규 구축 (17개 파일)
매각 사례 브리핑 PDF를 input 폴더에 넣고 `/blog`를 실행하면 네이버 블로그용 분석 글(HTML)을 자동 생성하는 신규 프로젝트를 설계·구축했습니다. superpowers 워크플로우로 설계서(`2026-07-20-blog-auto-design.md`)와 구현 계획서(`2026-07-20-blog-skill.md`)를 작성했고 — 글 구조는 개요표·건물 사진·거래 분석·실거래 평당가 비교·시사점·CTA 카드 순, 네이버 스마트에디터 붙여넣기 호환을 위해 이미지 base64 내장 단일 HTML로 완결하는 설계입니다(자동 등록은 약관 위반 위험으로 범위 제외). 스크립트 4종(extract_pdf.py, embed_images.py, render_card.py, seo_audit.py)과 전 글 공통 CTA 소개 카드(cta.png)를 만들고, 첫 검증 건인 우리은행(금호동) PDF로 건물 사진 추출과 썸네일 생성까지 진행했습니다.

## diwolbu web — 디월부 에디토리얼 발행과 요금제 데이터 갱신 (11개 파일)
구독 서비스 정보 사이트 디월부의 콘텐츠를 발행·준비했습니다. 뉴스로는 클로드 페이블 5 무료 프로모션 종료와 요금제 이원화(맥스 영구 포함·프로 크레딧 전환)를 7/20자로 발행하고, 쿠팡플레이 프리미엄 패스 광고·키미 K3 오픈 모델 가격 압박 뉴스와 칼럼 '무제한 시대의 종말', 꿀팁 'OTT 티어 다운그레이드'를 다음날(7/21) 발행분으로 미리 작성했습니다. 프로모션 게시물(갤럭시 S26 네이버플러스 1개월 무료 등)과 함께, 계산기에서 쓰는 서비스별 요금제 데이터(`lib/plans.ts`, `lib/services.ts`)를 2026년 7월 기준 최신 요금으로 갱신했습니다.

## wonbuilding AI TF — 베스핀글로벌 재견적서 검토 회의자료 작성 (2개 파일)
베스핀글로벌로부터 GCP 환경 세팅·에이전트 개발 구축 재견적서(PDF)를 수령하고, 개발팀 논의자료 HTML(모바일 단독 실행 규격)을 작성했습니다. 기존 1차 견적 대비 범위와 금액이 크게 확대된 통합 견적에 대해, 6개 구성 항목을 유지·분리(2차 발주)·제외(내부 개발)·감액(재협상)의 4단 프레임으로 판정하는 검토안을 정리했습니다. 핵심 방향은 NL2SQL 검색 엔진 골격만 이번 발주에 포함하고, 고객 매칭은 라벨 축적 후 2차 발주·보고서 생성은 내부 개발로 돌리며, 납품물을 완결형 챗봇 제품이 아닌 UI 독립적인 NL2SQL 도구(API)로 재정의하는 것입니다. 베스핀에 보낼 요구 명세·질문 목록과 담당·기한 배정 안건도 함께 담았습니다.
