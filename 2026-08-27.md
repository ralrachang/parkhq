# 데일리 작업로그 — 2026-08-27 (목)

> 전날(2026-08-27) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 3개 프로젝트, 140개 파일 변경.

## senior youtube — 유튜브 업로드 자동화 배선과 신규 영상 2편 클립 생성 (86개 파일)

factory 파이프라인에 YouTube 업로드 경로를 붙였습니다. `src/factory/upload.py`에 OAuth 인증(`ensure_auth`)과 영상 업로드를 구현하면서, 자막 첨부만 조용히 403으로 실패하던 원인이 `youtube.force-ssl` 스코프 누락이라는 것을 실측으로 확인해 스코프를 추가하고, 코드에 새 권한이 생겼는데 기존 토큰에 없으면 무인 실행이 알 수 없는 에러 대신 "재인증하라"며 멈추도록 했습니다. `cli.py`에는 `auth`·`upload`·`queue`·`preflight` 서브커맨드를 만들어 하루 한 편씩 올리는 큐를 구성했고, 이를 부르는 스케줄러 스크립트는 `.bat`이 UTF-8 한글 주석을 시스템 코드페이지로 오인하는 문제 때문에 PowerShell(`upload_daily.ps1`)로 작성했습니다. 첫 스케줄 실행은 OAuth 클라이언트 파일이 아직 없어 실패했고 그 기록이 `logs/upload_202608.log`에 남아 있으며, API로 올린 영상이 비공개로 잠기는 제약(감사 통과 전)을 풀기 위한 「YouTube API Services 감사 신청 초안」도 함께 정리했습니다. 콘텐츠 쪽으로는 '세계 최초의 우표 페니 블랙'과 '그로 미셸에서 캐번디시로 바뀐 바나나' 두 편의 대본을 쓰고 힉스필드로 각 11샷씩 클립을 뽑았는데, 크레딧이 예상과 어긋난 적이 있어 `generate_clips.py`가 샷 수 곱셈 대신 계정 잔액의 앞뒤 차이로 실소모를 보고하도록 고쳤습니다.

## Building scope — 건축물대장 대조 오라클 신설, 그 오라클이 엔진·감사표 결함을 연달아 적발 (30개 파일)

"정확도가 좋아졌는지 증명할 수단이 없다"는 판단에서, 정답 데이터를 늘리는 대신 건축물대장 표제부와 산출값을 대조하는 측정 오라클을 1순위로 착수했습니다. `lib/building-ledger.js`(조회·정규화)와 `lib/ledger-oracle.js`(순수 판정 규칙), 채점 하니스 `scripts/oracle-ledger.mjs`를 기존 엔진 코드 수정 없이 새로 만들고, 벤치 217주소를 프로덕션과 동일한 전처리로 전수 실행해 대지면적 대조가 성립한 건들의 평균·최대 편차를 새로 산출했습니다 — 5월 신뢰도표의 수치를 대체하는 결과이고, 당시 최대 편차의 정체는 관련지번이 여러 개라 애초에 비교 대상이 아니었던 필지였습니다. 개발 과정에서 오라클 자신이 틀린 경우를 열 건 잡아 전부 게이트 회귀로 박아뒀는데(게이트웨이 타임아웃을 영구실패로 오판, 층수·주차를 한도로 착각, 상한 비교를 적용값으로 함, 구법 필터를 사용승인일로 함 등), 오라클이 잡은 첫 실제 엔진 결함은 `lib/zone-mix-detect.js`가 용도지역 혼재 배분을 점 샘플 추정으로 만들어 놓고도 확정으로 표기해 경고·범위 병기·면적 입력 버튼을 통째로 꺼버리던 문제였습니다. 이를 `areaSource`(사용자 입력·다중필지 실면적=확정 / 점 샘플·미표기=추정)로 갈라 호출부 전수 배선했고, 이어서 감사표가 인용하던 법령 오류 2건(계단 유효너비를 규모 조건 없이 법정치로 표기, 피난규칙에 없는 출구 폭 규정을 인용)을 법제처 원문 대조로 정정했으며, 지구단위계획구역 검출 누락 6건을 LURIS 기준 보완 검출로 메우고 리포트에서 소멸 중이던 무고지 규제 코드 4종의 인용을 신설했습니다. 지역 커버리지는 손 입력(수원 1곳)으로는 확장이 안 돼 법제처 자치법규 API 자동 수집으로 전환해 경기 31개 시·군과 인천광역시 도시계획 조례를 확보했고 — 경기 필지를 서울 참조값으로 계산해 규모를 과소 산출하고 있었음이 드러났습니다 — 시행령 상한 대조·완전성 가드·단서 수치 방향 판정 3겹 안전장치와 검증 게이트를 함께 붙였습니다.

## diwolbu web — 하루치 에디토리얼 6건 발행과 인용 정책의 카탈로그 반영 (24개 파일)

에디터 페르소나별 하루 발행을 돌려 뉴스 3건, 프로모션 2건, 꿀팁 1건을 새로 실었습니다. 뉴스는 PS 플러스 9월 무료 게임 라인업 공식 발표를 다루며 발표 전 유출과 어긋난 편수·종료일을 대조해 "유출만 보고 해지를 결정하지 말 것"으로 정리했고, Z.ai의 새 코딩 모델 공개는 가중치 개방과 정액 구독 한도가 '월 몇 회'에서 '몇 시간 안에 얼마나'로 옮겨가는 흐름으로 읽었으며, 과기정통부 '모두의 AI' 발표평가 소식은 9월 베타를 확인하기 전까지 유료 구독 해지를 미루라는 조언으로 맺었습니다. 꿀팁은 지난달 지출을 복기하는 대신 다음 달 달력을 먼저 펴는 방식으로, 갱신일 파악 → 해지 후에도 기간말까지 이용 유지 → 크레딧·파일 선반출 → 즉시 해지가 오히려 유리한 예외 순으로 구성했습니다. 프로모션 2건(통신사 제휴 AI 구독 할인, 단말 구매 시 영상 구독 이용권)은 요금·기한·가입 조건과 함께 더 유리한 대안(학생 무료 혜택, 타 통신사 채널)을 병기했고, 기사에서 인용한 해지·환불·이월 정책이 카탈로그와 어긋나지 않도록 `lib/cancel.ts`와 `lib/details/`의 개별 서비스 문서(psplus·gemini·laftel·baemin·ssg7club), `lib/plans.ts`·`lib/services.ts`를 함께 갱신했습니다.
