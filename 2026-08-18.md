# 데일리 작업로그 — 2026-08-18 (화)

> 전날(2026-08-18) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 5개 프로젝트, 270개 파일 변경.

## senior youtube — 세계사 쇼츠 자동 제작 파이프라인 `factory`를 처음부터 구축 (120개 파일)
대본 마크다운 한 장을 넣으면 TTS 합성 → 샷 분할 → 영상 프롬프트 생성 → 자막(SRT/ASS) → 렌더 → SEO 메타데이터 → YouTube 업로드까지 이어지는 파이썬 패키지를 새로 만들었습니다. `build` / `prompts` / `setprompts` / `tts-test` / `auth` / `upload` 서브커맨드를 갖춘 CLI와 함께, TTS는 SAPI·Google·OpenAI 제공자를 인터페이스 뒤에서 교체할 수 있게 하고 영상 소스는 사람이 직접 만든 클립을 받는 `manual` 프로바이더로 두어, 나중에 Veo 자동 생성으로 바꿔도 호출부가 바뀌지 않도록 설계했습니다. 설정은 세 파일로 갈랐습니다 — `channel.json`(1080×1920·30fps 영상 규격, 자막 스타일, 샷 길이 4~8초), 채널의 얼굴에 해당하는 `visual_style.json`(색 문법, 카메라 워크, 실존 인물 클로즈업·화면 내 한글 금지 같은 하드 룰, 화면 유형별 프롬프트 템플릿), 그리고 TTS 발음 사전 `lexicon.json`입니다. 첫 콘텐츠로 호라즘 제국을 다룬 「상인 한 명 때문에 사라진 제국」 대본을 실제로 끝까지 돌려 87.3초·16샷짜리 영상과 커버를 뽑았고, 클립이 없는 샷은 자리표시 슬레이트로 채워져 길이·자막 싱크·SEO를 미리 검증할 수 있다는 점을 확인했습니다. 여기에 `TEST_3샷.md`를 따로 써서 지도·장면·데이터 세 유형만 골라 영상 생성 모델이 이 포맷을 재현할 수 있는지 판정하는 테스트를 설계했습니다.

## auto call — WonCall AI 통화녹음 CRM, 인증·업로드·전사·분석 파이프라인 완성 (82개 파일)
실제 통화녹음 5건으로 돌린 스파이크의 판정 문서를 먼저 확정하고, 그 결론 위에 앱을 얹은 하루였습니다. STT 경로는 비용이 들지 않고 통화 음성이 외부로 나가지 않는 로컬 whisper.cpp `large-v3-turbo`로 정했고, 실사용 데이터에서만 드러난 설계 결함들(임대 보증금 필드 부재, 희망가와 브리핑가 미구분, 가격 결정권자를 담을 자리 없음, 통화 기준 구분 재판정 결과 누락)을 분석 스키마·프롬프트 v2로 메웠습니다. 한 통화에 물건이 여러 개 나온다는 사실이 확인되어 `consultation_properties` 조인 테이블을 새로 두고, 실제 고객 통화가 들어가는 만큼 스펙과 달리 RLS를 켠 초기 마이그레이션을 작성했습니다. 앱 쪽은 Next.js 16 스캐폴딩(포트 3001), 이메일 매직링크 인증, 파일 드롭 시 파일명이 자동 분해되어 고객과 매칭되는 업로드 화면, 상담·job을 만드는 API, 그리고 Postgres 큐를 폴링하는 워커까지 붙였습니다. 전사문은 절대 수정하지 않는다는 원칙을 지키기 위해 근거 인용을 원문과 문자열 대조하는 `verifyEvidence`를 유지했고, 마지막에는 워커 로그가 실패를 "완료"라고 말하지 않게 하는 수정, 외부 프로세스 실패가 이유를 말하게 하는 수정, 매직링크 코드 교환의 주인을 하나로 모으는 수정을 실측 기반으로 넣었습니다.

## building sns — 청담동 근생빌딩 SOLD OUT 매각 사례 카드뉴스 발행 (51개 파일)
화·목에 운영하는 매각 사례 리뷰(SOLD OUT) 라인의 한 편을 처음부터 끝까지 돌렸습니다. 당일 촬영 사진 4장과 물건 브리핑 PDF를 입력으로 넣고, 후킹 → 물건 → 의문 → 해부 → 맥락 → 교훈 → 전환의 4막 골격에 맞춰 카드 10장을 구성했습니다. 감정가와 실제 거래가가 왜 벌어졌는지를 임대 수익이 아니라 대지 가치로 설명하고, 용적률이 이미 상한에 가까워 재건축 여지가 크지 않다는 점을 `chart` 카드로, 감정가와 실거래가의 성격 차이를 `comparison` 카드로 풀었습니다. 최종 산출물은 인스타·스레드용 1080×1350 PNG 10장과 카카오톡 소식용 중앙 정렬 변형 10장, 본문.md, 일상글.md입니다. 함께 손본 라인 설계 문서에는 브리핑 PDF의 비공개 항목(임차인 상호, 층별 임대 조건, 공실 여부, 위법건축물 표기)을 카드·본문 어디에도 쓰지 않고 수익률은 구간으로만 적는다는 원칙과, 차량 번호판은 반드시 가리되 옥외 간판은 가리지 않는다는 마스킹 정책을 정리해 두었습니다.

## blog auto — 종로구 당주동 근생빌딩 매각 사례 블로그 글 자동 생성 (13개 파일)
물건 브리핑 PDF 한 건을 넣어 네이버 블로그용 포스트를 통째로 뽑아내는 `/blog` 파이프라인을 실제 물건에 돌렸습니다. `extract_pdf.py`로 PDF에서 이미지를 추출한 뒤 지도·지적도·로고를 걸러내고 건물 외관 사진만 골라 본문에 배치했고, 위치·대지면적·연면적·용도지역·규모·준공·건폐율/용적률·접도조건·역세권 거리를 정리한 물건개요 표를 앞단에 세웠습니다. 초안(`draft.html`)에서 최종 포스트(`post.html`)로 다듬는 과정을 거쳤고, "이 건물이 팔린 이유 3가지"를 광화문역 초역세권 입지, 건물가액이 아닌 사대문안 토지 매입이라는 성격, 1층 앵커 임차인이 임대료 대부분을 책임지는 구조로 정리해 카드 이미지로 렌더했습니다. 썸네일과 CTA 카드까지 이미지 세트를 갖춰 그대로 업로드할 수 있는 상태로 만들었습니다.

## diwolbu web — 구독 카탈로그 정정 내용을 발행 완료 꿀팁에 소급 반영 (4개 파일)
브루·멜론·젠스파크 3종의 요금 정보가 실제와 어긋난 것을 카탈로그 감사에서 잡아내 `lib/plans.ts`·`lib/services.ts`와 각 서비스 상세 파일을 고쳤고, 그 정정이 이미 발행된 글에도 반영되도록 꿀팁 4편의 본문 수치를 소급해 수정했습니다. 대상은 「코딩 없이 앱 만드는 AI 세 개 켜두고…」(2026-07-15), 영상·음성 AI 중복 점검(2026-07-16), 하위 요금제 점검(2026-08-06), 환불 창구 점검(2026-08-07)입니다. 카탈로그만 고치고 본문을 그대로 두면 같은 서비스의 요금이 사이트 안에서 두 가지로 보이게 되므로, 발행 이후에도 숫자를 한 곳에 맞춰 유지하는 감사 루틴이 이날 실제로 작동한 셈입니다.
