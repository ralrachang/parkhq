# 데일리 작업로그 — 2026-08-04 (화)

> 전날(2026-08-04) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 5개 프로젝트, 261개 파일 변경.

## auto capcut — 하이퍼프레임 카드에 전환 애니메이션과 데이터 브리핑 시각 언어를 입혔습니다 (171개 파일)
전날 작성해 둔 설계·실행계획 문서(`docs/superpowers/specs/2026-08-03-hyperframe-motion-design.md`, `plans/2026-08-03-hyperframe-motion.md`)를 이날 실제 코드로 옮겼습니다. 카드가 하드컷으로만 바뀌던 문제를 `backend/render/blend.py`를 새로 만들어 해결했는데, "흐리게 미리 보이기" 방식 덕에 레이아웃이 고정되고 색만 바뀐다는 점을 이용해 Playwright 재캡처나 ffmpeg xfade 없이 상태 PNG 두 장을 PIL로 블렌딩하는 구조를 택했습니다. 카드는 statement·metric·chart·chapter 4종으로 갈라졌고, `analyze`가 주는 `kind`는 그대로 둔 채 렌더가 `layout`을 유도하도록 분리해 스키마 호환을 지켰으며, LLM이 지어낸 수치가 4K 화면에 박히지 않도록 자막 원문에 없는 숫자를 걸러내는 할루시네이션 게이트도 넣었습니다. 커밋 6건(metric 카드, chart·chapter 시각 시스템 적용, 캡처 배선 테스트 고정, 무음 컷에 걸린 카드가 통째로 사라지던 결함 수정, 캐시 매니페스트 렌더 시 본문이 비던 결함 수정, 죽은 토큰 제거)을 쌓고 실촬영 프로젝트를 다시 렌더해 전환 프레임과 최종 mp4까지 뽑았습니다 — 파일 수가 많은 건 대부분 이 렌더 산출물입니다.

## building sns — 화·목 SOLD OUT 라인 카드뉴스 한 세트를 기획부터 PNG까지 완주했습니다 (52개 파일)
신사동 매각 사례를 소재로 `output/cards/2026-08-04-sold-sinsa-641/` 아래 10장짜리 카드뉴스를 만들었습니다. `cards.json` 초안 → content-reviewer 검토본(`cards-revised.json`) → 확정본(`cards-final.json`)으로 이어지는 파이프라인을 그대로 태웠고, 인스타·스레드용 4:5 카드와 카카오톡 소식용 가운데 정렬본(`center/`)을 함께 생성했습니다. 사진 카드에는 차량 번호판 위치를 퍼센트 좌표로 지정해 블러를 걸었고, 같은 폴더의 `본문.md`에 스레드 토막·인스타 캡션·SEO 키워드와 함께 "직접 중개한 거래가 아닌 공개 자료 기반 제3자 분석"이라는 화자 규정과 데이터 한계 주석을 붙여 임대차 조건 같은 매도인 측 정보가 역산되지 않도록 정리했습니다.

## Building scope — "모르면 모른다고 말한다" 원칙을 코드와 발행 대기 콘텐츠 양쪽에 적용했습니다 (25개 파일)
정확도 관련 커밋 3건이 들어갔습니다. 대형 필지에서 세대 추정이 물리적으로 불가능한 전용면적 값을 내고 그게 국민주택규모 고지·주차 산정·평면 세대 라벨까지 흘러가던 문제를, 비현실 구간이면 추정을 포기하고(null) 사유를 고지하는 가드로 막았고, 이어서 그 판정 기준을 '전형 세대'가 아니라 최상층을 합친 '최대 세대(주인세대)'로 정정했습니다(`lib/dwelling.js`, `lib/calculate.js`, `lib/parking.js`). 여기에 `scripts/verify-dwelling-scale-guard.mjs`와 `scripts/verify-parking-live-0804.mjs`를 붙여 소형 필지 회귀 0과 라이브 조회 관통 검증을 각각 고정했습니다. 또 저장된 facts 스냅샷을 현재 엔진으로 재추출해 전 필드 비교하는 `check-facts-stale.mjs`를 만들어, 발행 대기 중이던 블로그 초안 4편과 스레드 초안 1편에서 조례 조항 인용 오류와 주차 대수 노후값을 찾아 함께 고쳤습니다.

## blog auto — 물건 브리핑 PDF에서 네이버 블로그용 사례 글을 자동 생성했습니다 (12개 파일)
`input/`에 넣은 매각 물건 브리핑 PDF를 `/blog` 스킬로 읽어 `output/커커빌딩/` 아래 초안(`draft.html`)과 발행본(`post.html`)을 만들었습니다. PDF 안의 현장 사진을 페이지별로 추출해 본문 이미지로 배치하고, 물건 개요·수익성 분석 표와 "이 건물이 거래된 이유 3가지" 요약 카드(`cards/reasons.html` → PNG), 썸네일까지 HTML로 렌더해 붙였습니다. 위법건축물 등재 여부와 신축 시 면적 감소, 임차인 구성이 단순해 공실 위험이 한 번에 온다는 점을 '매수 전 확인 사항'으로 따로 묶어, 사례 소개가 매수 권유로 읽히지 않도록 구성했습니다.

## wonbuilding AI TF — 외주 개발사 기술 협의용 미팅 어젠다를 보고서 형식으로 정리했습니다 (1개 파일)
이날 예정된 베스핀글로벌 기술 협의를 앞두고 `26.08.04 베스핀글로벌_기술협의_미팅어젠다_보고서판_v1.0.html`을 작성했습니다. 문서의 중심은 금액 흥정이 아니라 발주 경계 확정으로, "외주사는 UI에 종속되지 않는 NL2SQL API(도구)를 납품하고, 검색창·챗봇 같은 표면과 오케스트레이터는 우리가 소유한다"는 선을 세운 뒤 그 선에서 견적 항목별 판정(매칭 기능은 데이터 라벨 축적 후 Wave 2로 이동, PM 공수는 스코프 비례 감액)이 도출되도록 구성했습니다. 스코프를 줄일 때마다 할인폭도 함께 줄어 감액 효과가 상쇄되던 흐름을 표로 드러내고 할인율 하한 고정을 재견적 조건으로 명시하도록 정리했으며(구체 금액·할인율은 문서 내부에만 기재), NDA 미체결 상태에서 넘기지 않을 자료 범위, 실무자에게 그 자리에서 답을 받을 기술 안건 A~K, 세 번째 미회신 중인 권한·로그·옵션 견적을 포함한 서면 수령 목록을 시간 배분과 함께 담았습니다.
