# 데일리 작업로그 — 2026-08-14 (금)

> 전날(2026-08-14) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 6개 프로젝트, 435개 파일 변경.

## Auto IM2 — 항아리 상권 추천물건 6선 주간 다이제스트 제작 (298개 파일)
서울 6개 자치구의 이른바 '항아리 상권' 빌딩 6건(케이빌딩·다보빌딩·에이투블레스·신내동 태흥빌딩·삼경빌딩·제이타워) PDF를 일괄 파싱해, 물건별 개요·강점·엑싯플랜·임대현황을 담은 데이터 브리핑 `_brief.md`와 인덱스를 만들었습니다. 추출 과정에서 원본 PDF에 적힌 수익률 한 건이 자체 계산식과 맞지 않는 것을 발견해 재계산으로 정정하고, 그에 따라 물건 노출 순서를 수익률 내림차순으로 재배열했습니다. 6개 물건에서 뽑아낸 이미지 279장을 슬라이드용으로 정리했고, 브랜드북 스타일 HTML을 생성한 뒤 레이아웃 넘침 자동 검증을 돌려 지적 사항 0건을 확인하고 최종 PDF까지 출력했습니다.

## auto call — WonCall AI, 스파이크 판정에서 전사·분석 파이프라인 구현까지 (71개 파일)
실제 통화녹음 5건으로 돌린 스파이크 결과를 `2026-08-14-spike-verdict.md`로 판정 정리했습니다. STT 품질은 통과했지만 임대 보증금 필드 부재, 희망가와 브리핑가 미구분, 가격 결정권자 자리 없음, 통화 기준 재판정 결과 미보관 등 실사용 데이터로만 드러난 설계 결함 4건을 스키마·프롬프트 v2로 해결했고, "한 통화에 물건이 여러 개 등장한다"는 발견은 DB를 1:N으로 바꾸는 근거가 됐습니다. 이어 Day 3~6 구현 계획서를 쓰고 그대로 실행해 Next.js 16 스캐폴딩, 9개 테이블 마이그레이션(RLS 포함), 이메일 매직링크 인증, 업로드 화면과 상담·job 생성 API, whisper.cpp 로컬 전사와 워커 폴링 루프까지 하루에 22커밋으로 연결했습니다. 전사문은 절대 수정하지 않고 `verifyEvidence`의 원문 대조 검증을 지키는 원칙을 계획서와 코드 양쪽에 못박았습니다.

## Building scope — 평면도 v3 세대 칩 배치 교정과 비도시지역 조례값 입력 (36개 파일)
평면도의 세대 칩이 합성 bbox 좌상단에 고정돼 L자·다중 폴리곤 세대에서 코어·복도·벽 위에 떠 있던 결함을 실측(119건 중 53건)으로 확인하고, 조각별 배치 탐색 → 2줄 접기 → 여백 라벨+지시선 순의 폴백으로 고쳐 넘침 0건을 만들었습니다. 같이 걸린 표제란 정규식 오류(강등 고지가 4줄 컷에서 사라지던 문제)도 표현이 아닌 사실 기준 패턴으로 바꿨고, 신설 게이트 `verify-floorplan-chip.mjs`는 렌더러 자체 신고를 믿지 않고 기하를 재계산하도록 짰습니다. 정확도 쪽에서는 비도시지역 조례값을 "입력해 달라"고만 하고 입력할 곳이 없던 막다른 안내를 모달 UI로 해소했고(`lib/patch-nonurban.js`), 다중필지 합산이 비도시지역을 '용도지역 미확인'으로 오분류하고 조례값을 무시하던 결함 세 갈래를 잡았습니다. `verify-nonurban-spa` 41/0, `verify-merge-nonurban` 36/0 등 신규 게이트를 함께 붙였습니다.

## diwolbu web — 데일리 발행과 서비스 카탈로그 정정 (19개 파일)
구독 정보 사이트의 하루치 콘텐츠를 발행했습니다. 뉴스 3건(제미나이 3.7 플래시 공개, OpenAI 유료 한도 리셋, 트위치·아마존 AI 학습 옵트아웃), 꿀팁 1건(한집에서 중복 결제 중인 구독을 가족 요금제로 묶는 법), 칼럼 1건('나는 이 구독에 어느 문으로 들어왔나' — 웹·앱스토어·통신사·제휴 중 어느 결제 경로로 가입했느냐가 값뿐 아니라 해지 방법과 인상 통보 방식까지 정한다는 논지)에 프로모션 신규 2건과 기한 지난 프로모션 4건 정정을 더했습니다. 이어 카탈로그 감사에서 Replit·Suno·Cursor·M365 코파일럿·HeyGen 5종의 요금제 설명을 최신 구조에 맞게 고쳤는데, 특히 Cursor는 '사용량 크레딧' 방식으로 바뀐 포함량 구조(자체 모델과 외부 모델의 포함분이 따로 계산되는 점)를 풀어 썼습니다.

## naver land — 수집 주기 확대와 '새 주소' 그룹 표시 (7개 파일)
네이버 부동산 토지·건물 매매 매물 모니터의 실행 스케줄을 하루 3회(09/13/16 KST)로 늘리고, cron 예시와 README를 함께 갱신했습니다. 🆕 새 주소 필터를 켰을 때 최근 2일치 수집분을 배치별 헤더로 묶어 보여주도록 `store.py`·`generate.py`·`pipeline.py`를 손봤고, 재생성(`--regenerate`)이 '마지막 수집' 시각에 재생성 시각을 덮어쓰던 버그를 고쳤습니다. 설정에는 새 주소 표시 창을 조절하는 `new_location_window_days` 항목이 추가됐습니다.

## wonbuilding AI TF — 파트너사 NDA·용역계약 초안 검토 반영 (4개 파일)
전날 버전(0813 수정안)에 법률 검토 코멘트 형태로 달려 있던 제안들을 실제 조문으로 반영한 0814 수정안을 NDA와 컨설팅 용역계약서 양쪽에 만들었습니다. 용역계약서는 결과물 검수에서 무통보 시 승인 간주 규정과 통보 기한을 명문화하고, 하자보수 조항을 구체화했으며, 결과물 지식재산권에 '양도' 표현과 2차적저작물작성권을 명시하고 제3자·오픈소스 지식재산이 결합된 경우의 재실시 허락 범위를 보완했습니다. 비밀유지와 손해배상 조항은 중대한 위반 유형을 열거하고 위약벌 조항을 신설하는 방향으로 재구성했으며, NDA는 존속기간·권리귀속·반환 의무를 용역계약서와 서로 어긋나지 않게 맞췄습니다.
