# 데일리 작업로그 — 2026-07-09 (목)

> 전날(2026-07-09) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 7개 프로젝트, 417개 파일 변경.

## howmuch.go — 실거래 수집 범위 확장(A)과 적응형 deal_weight(B) 구현·백테스트 튜닝 (300개 파일)
「희소 표본·수집 범위 개선(A+B)」 기획서와 구현 설계 스펙을 작성하고, 매매 추정의 국토부 실거래 수집을 본건 단일 법정동에서 같은 구의 반경 내 법정동들로 확장하는 A단계를 서브에이전트 주도 개발(SDD) 태스크(A1~A9r)로 구현했다. fetch_molit 구 전체 페이지네이션, 동 선별 모듈 dong_scope 신설, fetch_bldg 동 파라미터화, match_geocode 다동 매칭 등 파이프라인 모듈과 대응 테스트 6종을 수정·신설했고, B단계(B1)로 고정 0.7이던 실거래 가중치를 표본 수에 따라 포화하는 적응형 deal_weight(w_max·min(1, n/K), 기본 off 시 산출 바이트 동일)로 교체했다. 이어 K 후보(k3/k4/k6/k8)·컷오프 등 파라미터 후보별 백테스트를 여러 차례 돌려 튜닝하고, 주소 슬러그별 매매 리포트(report-sale.html·지도 캡처)를 재생성했다.

## sise-web — 인접 법정동 comp 확장 엔진 완성 + 토스 톤 UI 리디자인 기획 (85개 파일)
엔진과 화면 두 갈래로 진행했다. 엔진 쪽은 locate_trades의 "대상 법정동만" 제약을 풀어 반경 800m 하드컷·1500m 근접게이트로 인접 동 통건물 실거래를 매매 비교군(comp) pool에 포함시키는 계획을 세우고, kakao 동 해석(resolve_dong)·멀티동 locate·파이프라인 배선을 feat/adjacent-dong-comp 브랜치에서 구현했으며, 최종 리뷰 fix wave로 교차동 note를 채택 comp 기준으로 바꾸고 동 해석 실패 시 해당 동만 건너뛰는 예외 가드를 추가했다(신규 RED 테스트 포함, 기존 스위트 green 유지). 화면 쪽은 검색·로그인/대시보드/이력 3화면을 토스 톤으로 개편하는 기획서·설계 스펙과 시각 정본이 될 데스크톱·모바일 목업 HTML을 작성했다.

## naver land — 매물 모니터 코드 정비와 일일 크롤 실행 (11개 파일)
네이버 부동산 매물 모니터의 앱 모듈(client·config·pipeline·regions·deploy)과 엔트리포인트 run.py, config.toml, 지역 데이터를 정비했다. 당일 실행 로그 기준 서초구·송파구 등 대상 37개 동을 전체 스캔해 665건을 목격하고 신규 416건을 저장했으며, 누적 3,760건으로 정적 사이트(site/index.html)를 재생성했다(외부 배포는 config에서 비활성 상태로 건너뜀).

## Auto IM2 — 논현동 신축빌딩 IM 브랜드북 제작·레이아웃 QA·PDF 출력 (7개 파일)
논현동 신축 빌딩 매물의 IM(투자 소개 자료) 제작을 진행했다. 분류·초안 데이터(02_classified·04_draft)를 갱신하고 브랜드북 스타일 HTML과 이미지 임베드본을 생성했으며, 레이아웃 자동 검증에서 5번 슬라이드의 세로 넘침과 오버레이 요소(태그·타이틀) 이탈을 리포트와 플래그 스크린샷으로 검출·기록했다. 최종본은 PDF 출력까지 완료했다.

## Building scope — 비서울 필지 조례 오적용(용적률 과대 산출) 버그 수정 (7개 파일)
규모검토 MVP의 건폐율·용적률 표(zoning.js)가 서울시 도시계획조례 §44·§48 값인데 전국 주소에 구분 없이 적용돼, 서울 한정 한시 용적률 완화(2종일반 200→250% 등)가 비서울 필지에도 붙어 용적률이 50%P 과대 산출되던 버그를 고쳤다. 필지 주소로 시·도를 판정해 비서울이면 완화를 미적용(법정 기본값)하고 결과가 "서울시 조례 참조값"임을 notes·경고·감사 NOTE에 명시하도록 했으며, 이 표기를 calculate·zone-mix·lawcheck 렌더러·HTML/SVG 출력까지 전파했다. 비서울/서울 회귀/주소 미상/비정형 주소 케이스를 검증하는 전용 스크립트(verify-region-ordinance.mjs)를 추가하고 가로구역 높이 검증 스크립트(verify-streetblock-height.mjs)도 함께 손봤다.

## diwolbu web — 뉴스 3건·꿀팁 1건 하루치 콘텐츠 발행 (4개 파일)
구독 절약 미디어 '디월부'의 하루치 콘텐츠 4건을 JSON 포맷(요약·본문·출처 포함)으로 발행했다. 뉴스는 GPT-5.6(Sol·Terra·Luna) 일반 공개 시작, SKT 5G·LTE 통합요금제 '베스트·라이트' 출시와 가족결합 완화, 티빙·웨이브 합병 표류 속 연말까지 요금 동결 3건이고, 꿀팁은 Xbox Game Pass·PS Plus 이중 구독을 실제 플레이 시간 기준으로 점검·정리하는 가이드 1건이다.

## wonbuilding AI TF — 파트너사 견적 자료 수령·정리 (3개 파일)
클라우드 파트너사로부터 전달받은 견적 문서 2건(GE 설치 견적서, 에이전트 GCP 아키텍처 견적서)을 압축본과 함께 프로젝트 폴더에 수령해 정리했다. 회사 AI TF의 에이전트 인프라 도입 검토에 쓰일 견적 기준 자료로, 구체 금액·조건은 원본 문서로 관리한다.
