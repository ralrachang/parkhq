# 데일리 작업로그 — 2026-07-27 (월)

> 전날(2026-07-27) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 6개 프로젝트, 221개 파일 변경.

## auto capcut — 영상 자동편집 파이프라인을 4K 실촬영본으로 처음부터 끝까지 완주 (113개 파일)
백엔드 전 구간을 손봤습니다. `config.py`에는 무음 컷 판정 파라미터(`min_speechless`·`tail_pad`)를 추가해 촬영 종료 조작음이 살아남거나 종결어미가 잘리는 실측 문제를 잡았고, `data/molit.py`에 국토교통부 아파트 실거래가 API 클라이언트를 붙여 "서울" 같은 시 단위 요청을 25개 구로 분해·합산하도록 했습니다(WAF가 기본 UA를 막는 문제와 개발계정 일일 호출 한도까지 코드에 반영). `pipeline/charts.py`는 밝은 영상 위에서 차트 글자가 사라지는 현상을 figure 전체 반투명 패널로 해결했고, `pipeline/thumbnail.py`는 라플라시안 분산으로 베스트 프레임 3장을 뽑아 자동 줄바꿈(`fit_thumb_text`)까지 처리합니다. 여기에 2026 썸네일 트렌드 규칙(3단어 이하·단일 초점·좌측 2/3 텍스트존)을 적용한 시안 생성기 `_generator_v2.py`로 N1~N3 세 방향을 만들고 320px 축소본으로 모바일 가독성까지 검증했으며, 4K 원본에서 자막이 절반 크기로 보이는 문제는 `config-4k.json` 프리셋(자막·외곽선·하단여백 2배)으로 분리했습니다. 실제 촬영본 약 122초를 돌려 챕터 5개·자막 ASS·차트 오버레이가 붙은 `final.mp4`와 쇼츠 2편까지 산출했습니다.

## building sns — '호가 vs 실거래가' 카드뉴스 10장을 3차 리비전까지 완성 (45개 파일)
매도인이 호가를 정하는 방식을 다룬 카드뉴스 세트(`2026-07-27-hoga`)를 만들었습니다. 표지·본문·비교·수치·체크리스트·CTA 등 10장 구성을 `cards.json` → `cards-revised.json` → `cards-final.json` 3단계로 카피를 다듬었는데, 주로 표지 서브라인과 수치 카드 문장의 어미·강조 문구를 조정하는 작업이었습니다. 1080×1350 HTML을 먼저 렌더한 뒤 PNG로 뽑는 방식이고, 같은 내용을 중앙정렬로 재조판한 `center/` 변형 10장을 카카오톡 소식용으로 추가했습니다. `본문.md`에는 카드의 근거가 되는 주제 요약(실거래 신고 기한과 공개시스템, 오래 걸린 매물이 받는 인식, 복수 중개 노출의 역효과)과 스레드용 버전을 함께 정리해 뒀습니다.

## Auto IM2 — 다물건 브리핑 자료를 브랜드북 스타일 24페이지로 제작·검수 (28개 파일)
5개 물건을 하나로 묶는 브리핑 자료를 브랜드북 단일 스타일로 제작했습니다. 물건을 '즉시인수'와 '전환형' 두 트랙으로 나누고, 물건마다 4페이지(개요 → 입지·지도 → 운영·수익 → 엑싯플랜) 구조를 적용했으며, 전환형은 운영 페이지를 임대현황+전환 시나리오로 대체했습니다. 사용자 요청으로 원본 PDF의 카카오맵을 물건당 광역·근접·지적도 3종으로 페이지 단위 크롭해 지도 페이지를 새로 만들었고, 수익률은 산정 기준(매출 기준인지 순수익 기준인지)을 반드시 병기하도록 했습니다. `verify_layout.py` 자동 검증 0이슈 + 24장 전 슬라이드 스크린샷 육안 확인을 거쳐 PDF로 출력했고, 매도자 측 자료가 확보되지 않은 항목은 `run_summary.md`에 'DATA GAP'으로 명시해 후속 과제로 남겼습니다.

## Building scope — 서울 한시 용적률 완화 운영기준 원문을 엔진에 정확히 반영 (23개 파일)
서울시 「제2·3종일반주거지역 소규모건축물 용적률 완화 세부 운영기준」 PDF를 추출해 `docs/legal/seoul-far-relax-guideline-2025.md`에 요약 없이 원문 그대로 보존하고, 이를 근거로 엔진 3건을 정정했습니다. 기존에 '조례 부칙'으로 잘못 인용하던 근거를 조례 §51②9로 바로잡고 적용기한(2028-05-18) 경과 시 법정 용적률로 자동 강등하도록 했으며, 중첩 완화 상한(시행령 최대한도의 1.2배)과 국민주택규모 85㎡ 요건을 추가했습니다. 다만 85㎡ 초과 세대가 있어도 용적률을 임의로 깎지 않고 경고·표기만 남기는 쪽을 택했는데, 이는 "검증하지 않은 것을 검증한 척하지 않는다"는 프로젝트 원칙에 맞춘 판단이고 `verify-far-relax-guideline.mjs`에 9개 시나리오 회귀 테스트로 고정했습니다. 콘텐츠 쪽으로는 블로그 큐 6편(압구정 일조 면제 근거, 창신동 지구단위계획, 충무로 도심 감축, 사당동 7층 캡, 신사동 북측도로 완화, 무료 가설계의 함정)과 스레드 큐 7편을 정비하고, `threads-format-guide.md`로 5단 템플릿과 정직성 가드레일을 기준선으로 확정했습니다.

## diwolbu web — 7월 27일자 뉴스·프로모션·팁 콘텐츠 세트 발행 (10개 파일)
당일자 뉴스 4건(오픈AI 평가용 모델의 샌드박스 이탈과 허깅페이스 침입, 오픈AI 연쇄 장애, 갤럭시 AI 구독클럽 요금 공개, PS플러스 8월 카탈로그 변동), 프로모션 2건(갤럭시 XR 익스플로러 팩, KT 콘텐츠페이의 애플 구독 청구할인), 팁 1건(디자인 툴 구독 스택 다운사이징)을 JSON으로 작성했습니다. 이전 회차 항목 3건도 함께 손봤습니다. 각 항목은 `title`/`summary`/`body` 외에 `impact`(뉴스) 또는 `note`(프로모션) 필드를 따로 두어 "그래서 지금 뭘 해야 하는가"와 "무엇을 직접 확인해야 하는가"를 본문에서 분리했고, 특히 프로모션은 할인 조건이 회차마다 달라질 수 있다는 점과 청구 반영 시점을 별도로 경고하는 형태로 정리했습니다.

## blog auto — 빌딩 매매 사례 블로그 포스트 초안·발행본 생성 (2개 파일)
방배역 인근 코너 건물의 매매 사례를 다룬 블로그 포스트를 만들었습니다. 수익률만 보면 설명되지 않는 거래인데 매도인 명도 조건이 붙어 있었다는 점을 축으로 삼아, 위치·대지면적·연면적·용도지역·준공연도·건폐율/용적률을 정리한 물건개요 표에서 시작해 숫자를 따라가는 구성입니다. `draft.html`로 본문을 확정한 뒤 이미지와 카드를 임베드한 `post.html`(약 1.2MB)을 발행본으로 생성했습니다.
