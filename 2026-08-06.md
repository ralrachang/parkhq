# 데일리 작업로그 — 2026-08-06 (목)

> 전날(2026-08-06) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 3개 프로젝트, 516개 파일 변경.

## auto capcut — 하이퍼프레임 카드의 신뢰성을 감사해 차트를 걷어내고 발화 근거 지표 카드로 대체 (412개 파일)
전날 커밋이 만든 시각 회귀를 4K 실촬영본 재렌더로 잡았습니다. 카드가 교체될 때마다 브랜드 워터마크가 두 번 그려졌는데, `push_to`가 본문 사각형 바깥을 읽어 아래쪽 브랜드 잉크를 끌어온 것이 원인이라 `blend.py`에 `_shift_up`을 새로 만들어 사각형 안만 읽고 비는 줄은 마지막 행 복제로 메우도록 고쳤습니다(e087836). 이어 "초반에 나오는 그래프가 확실한 정보냐"는 질문에서 출발한 감사로 차트를 전부 제거했습니다 — 발화는 주간 가격지수인데 화면은 월별 평균 실거래 금액이었고, `_nearest_chart`가 거리 제한 없이 하나뿐인 차트를 모든 chart 카드에 붙이고 있었으며, 차트 자체가 이전 분석 시점의 잔류물이었습니다. 차트를 빼자 본문이 빈 카드 18초가 드러나 `prompts/analyze.md`가 chart 프레임에도 `lines`를 채우게 하고 `card_html`에 헤드라인 승격 폴백을 넣었으며(5c34d96), 숫자가 발화보다 3~5초 먼저 뜨던 스포일러는 `_metric_reveal_times` 신설로 "그 숫자를 실제로 말하는 cue"에 맞춰 켜지게 고쳤습니다(4ead0ca, 테스트 436 passed 3 skipped).

## Auto IM2 — 빌딩 IM 3건을 PDF 확정까지 완주 (신규 2건·개정 1건) (103개 파일)
메종드마리 북촌은 브랜드북 스타일 15페이지로 신규 제작했습니다. 인터뷰 결과를 반영해 임차인 슬라이드를 KPI 직후로 전진 배치하고, 사용자가 추가로 제공한 조감도를 근거로 THE TRANSFORMATION 슬라이드를 신설했으며, 조감도가 원자료 PDF에 없는 내용이라는 점과 인허가 확인 필요를 검토사항·면책에 그대로 밝혔습니다. 해창빌딩(루원시티)은 코퍼레이트 스타일 14페이지로, 임차 구성의 분산과 배후 수요를 논리 축으로 세우되 역세권이 아닌 점·공부상 용도 불일치·원자료 간 수치 불일치 같은 약점은 감추지 않고 검토사항으로 명시하는 방식을 택했습니다. 이문동 컴포즈커피는 매도자 측 매매가 조정을 반영한 Rev.2 개정으로 슬라이드 구성과 디자인은 Rev.1 그대로 두고 매매가에서 파생되는 수치만 전량 재계산했으며(기존 수치가 같은 계산식으로 재현되는지 먼저 검산), 세 건 모두 `scripts/verify_layout.py` 레이아웃 검증과 슬라이드별 캡처·PDF 렌더 검수를 마쳤습니다.

## diwolbu web — 구독 등급 다운그레이드 팁 콘텐츠 1편 추가 (1개 파일)
`content/tips/2026-08-06-lower-tier-plan-check.json`을 새로 작성했습니다. 요금표에서 굵게 강조되는 칸 아래에 숨어 있는 하위 등급을 짚어, 구독을 하나도 해지하지 않고 등급만 내려 고정 지출을 줄이는 방법을 AI·창작·영상·음성 서비스별로 정리한 6분 분량 글입니다. 진단 기준(지난달 한도 경고 여부, 상위 등급 전용 기능 사용 횟수)부터 서비스별 등급 변경 경로, 내렸을 때 빠지는 것(저장공간 축소·크레딧 한도·고급 기능 제외)과 되돌리기 가능 여부까지 주의사항으로 담았습니다.
