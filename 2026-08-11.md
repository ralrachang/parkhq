# 데일리 작업로그 — 2026-08-11 (화)

> 전날(2026-08-11) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 3개 프로젝트, 53개 파일 변경.

## diwolbu web — 에디토리얼 데일리 발행 + 서비스 카탈로그·해지 가이드 확충 (23개 파일)
구독 관리 서비스 '디월부'의 하루치 콘텐츠를 발행했습니다. 뉴스 3건('나는 솔로' 넷플릭스 단독 스트리밍 이동에 따른 웨이브·티빙·쿠팡플레이 점검, 서울 청년 AI 바우처, 영국 구독 쿨링오프 제도), 프로모션 2건(KT 콘텐츠팩 앱스토어 혜택, SKT '월간 럭키 페스타' 청구할인 — 보도기사와 공식 이벤트 데이터의 기간 표기 차이를 교차 확인해 바로잡음), 꿀팁 1건(인당 결제 '팀 시트' 점검 가이드 team-seat-audit)을 새로 썼고, 기존 팁·프로모 5건도 함께 손봤습니다. 콘텐츠를 뒷받침하는 데이터 쪽에서는 요금 카탈로그(lib/plans.ts·services.ts)에 Claude Team 등 인당 결제 시트 플랜을 보강하고, 서비스 상세 페이지 7종(claude·envato·kling·notion·speak·vrew·youtube)과 서비스별 해지 가이드(lib/cancel.ts)를 갱신했습니다.

## Building scope — 교차로 관통 도로폭 오측정 차단 + 평면도 v3 스파이크 산출 (22개 파일)
사전 규모검토 엔진(짓다/BuildScope)에서 정북일조 판정의 거짓 면제 버그를 잡았습니다. 실제 필지 라이브 검증 중, 접면 도로를 재는 ray가 교차로를 대각선으로 관통해 건너편 도로까지 연쇄 병합되면서 15m 도로가 36.9m 광로로 측정되고 §86② 정북일조 면제가 잘못 붙는 사례를 발견 — 접면 도로필지 기여분의 3점 안정성(abuttingStable) 가드를 lib/road-width-cadastral.js·road-setback.js에 추가하고, §86② 광로 배제 판정과 §86⑥1 북측 개재물 완화 게이트에만 적용해 §46 후퇴·도면 라벨 등 기존 경로는 회귀 없이 보존했습니다. 교차로 관통·적층 도로·단일 도로·쪼개진 도로 케이스를 검증하는 스크립트 verify-sunlight-junction-chain.mjs를 새로 작성했습니다. 평면도 쪽에서는 v3 스파이크 엔진(실제 필지 폴리곤 + 산정표 층면적 + 정북사선 후퇴 + 권장 세대수·승강기 검증값 기반)으로 신사동·인사동 두 필지의 1F/기준층 도면(SVG·PNG)을 뽑아, 구 CAD 프로토·현행 v2와 나란히 비교하는 compare.html을 갱신했습니다. 마케팅 소재 큐(queue.json)도 점검해 facts 근거가 확보되지 않은 소재의 보류 사유를 업데이트했습니다.

## auto capcut — 하이퍼프레임(발화 → 메인 화면 카드) 렌더 파이프라인 작업 (8개 파일)
유튜브 영상 자동 편집 파이프라인에서 발화 내용을 메인 화면 카드로 띄우는 '하이퍼프레임' 단계를 다듬었습니다. 분석 프롬프트(prompts/analyze.md)에 hyperframes 출력 스키마와 작성 지침(헤드라인 6단어 이하·수치 금지, 이어 읽으면 한 문장이 되는 계단식 lines, 발화에 실제 나온 수치만 metrics로)을 정비하고, backend/pipeline/hyperframe.py에서 프레임 스펙 정리와 수치 검증(부분문자열 오탐을 막는 숫자 통째 토큰 비교)을 구현했습니다. 렌더 쪽(backend/render/frames.py·blend.py)에서는 카드 전환 프레임 합성을 손봐, 제자리 페이드는 대칭 smoothstep 이징으로 바꿔 "전환이 짧다"고 느껴지던 원인을 해소하고, 실패 시 밀림→디졸브→하드컷으로 단계 강등되는 graceful 계약을 유지했습니다. 네 단계 각각의 테스트(test_analyze·test_blend·test_frames·test_hyperframe)도 함께 갱신했습니다.
