# 데일리 작업로그 — 2026-06-16 (화)

> 전날(2026-06-16) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 5개 프로젝트, 126개 파일 변경.

## building sns — "빌딩 밸류업의 본질" 카드뉴스 2세트 제작 (48개 파일)
"신축이 아니라 싸게 사서 가치를 올리는 것"이라는 주제로 `2026-06-16-valueup` 카드뉴스 9장을 제작했다. 평당 건축비 1,000만원 시대의 무분별한 신축 위험성, 강남 신축 과공급→공실 역설, 명도 불가 물건의 재임대만으로 수익률 4% 회복 사례, 엘리베이터 신설·임차 재구성 등 저비용 밸류업 레버를 카드별로 풀었다. HTML 초안(`_html/card-01~09.html`)과 최종 PNG, `cards.json` → `cards-revised.json` → `cards-final.json` 개정 흐름, Threads·인스타 캡션이 담긴 `본문.md`까지 완성했으며, 다음날 게시 예정인 `2026-06-17-imjang` 임장 카드뉴스도 동일한 파이프라인으로 함께 착수했다.

## Taxpago — M4 월간 리포트를 세금 캘린더로 재설계하고 구현 완료 (37개 파일)
기존 "월별 리포트(월×10% 부가세, 월×12 연환산)" 설계가 실제 신고 방식과 동떨어진다는 피드백을 받아, `/reports` 화면을 **연도별 세금 캘린더**로 전면 재설계했다(`2026-06-16-m4-monthly-report-design.md`). 개인 일반과세자 기준 부가가치세 1기(7/25)·2기(익년 1/25), 종합소득세+지방소득세(익년 5/31) 일정을 `lib/tax/calendar.ts`의 `buildTaxCalendar`로 구현하고, `lib/tax/estimate.ts`에 `vatRange`·`annualIncomeTaxRange`·`progressiveIncomeTax` 순수 함수를 추가했다. `tests/tax.test.ts`·`tests/calendar.test.ts`로 단위 테스트를 작성했고, 실제 카카오뱅크 거래내역 xlsx 파일을 업로드해 파싱·분류 흐름까지 E2E 검증했다.

## worldcup dashboard — 2026 북중미 월드컵 실시간 대시보드 신규 구축 (21개 파일)
네이버 검색 API를 파싱해 경기 결과·하이라이트·일정·조별 순위를 보여주는 단일 페이지 대시보드를 처음부터 구축했다. Express 서버(`server/server.js`)에 `/api/results`, `/api/highlights`, `/api/fixtures`, `/api/standings` 엔드포인트를 구성하고, `parser.js`의 스코어 정규식 파싱과 `cache.js`의 5분 TTL 인메모리 캐시로 쿼터를 보호했다. API 키 없이도 `server/seed/players.json` 등 시드 데이터로 즉시 데모 가능하도록 설계했으며, Football Manager 2026풍 다크 네이비+시안 디자인을 `tokens.css`로 토큰화하고 팀별 상세 페이지(`team.html`·`js/team.js`)도 함께 추가했다.

## dungeon writer — 텍스트 RPG "던전 기록소" Day 0 기획 패키지 완성 (14개 파일)
패키지형 싱글플레이 텍스트 RPG 프로젝트 방향을 확정하고(`devlogs/2026-06-16.md`), 기획 문서 7종(`GAME_SPEC.md`, `DEVELOPMENT_RULES.md`, `WRITING_GUIDE.md`, `QA_CHECKLIST.md`, `RELEASE_PLAN.md` 등)과 AI 역할 프롬프트 4종(`claude_code_developer.md`, `claude_writer.md`, `claude_editor.md`, `claude_qa.md`)을 한 번에 작성했다. 핵심 세계관("기록이 현실을 덮어쓴다", "마왕은 존재한 적 없다")과 엔딩 분기 수치(`corruption`/`erasure`/`truth`)를 확정하고, M1 플레이 흐름인 `chapter01_npcs.json`·`endings.json` 샘플 콘텐츠도 초안을 완성했다.

## Building scope — 사업성(Option C) 위저드 구현 및 ROI 상수 결함 해소 (6개 파일)
토지 매입비를 사용자 입력으로 받아 ROI를 실제값화하는 "Option C" 사업성 위저드를 `lib/feasibility.js`에 구현했다. 기존 고정 단가 기반 ROI 66.7% 상수 결함을 해소해, 토지비 미입력 시 ROI를 `null`로 정직하게 표시하고 입력 시 `사업이익 = 분양수입 − (토지비+공사비+제경비)` 산식으로 계산하도록 교체했다. `verify-feasibility.mjs`에서 단위 검증(토지비에 따른 ROI 변동, 지하 분양수입 제외 버그픽스)과 서버 E2E 검증(`/api/analyze`) 두 층위로 테스트하고, `verify-floorplan-v3.mjs`·`verify-siteregs.mjs`도 함께 갱신했다.
