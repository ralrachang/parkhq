# 데일리 작업로그 — 2026-06-15 (월)

> 전날(2026-06-15) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 4개 프로젝트, 138개 파일 변경.

## Taxpago — Next.js 기반 세금·매출 리포트 SaaS 초기 프로젝트 셋업 (87개 파일)

사장님을 위한 AI 매출·세금 리포트 SaaS인 Taxpago의 프로젝트 골격을 구축했다. Next.js 16 + TypeScript + Tailwind v4 + Prisma 7(SQLite) + NextAuth v5 스택으로 인증 플로우(로그인·회원가입·비밀번호 재설정), 앱 사이드바(`app-sidebar.tsx`), 온보딩(`onboarding/page.tsx`), 사업장 설정(`settings/business/page.tsx`) 등 핵심 페이지와 UI 컴포넌트를 일괄 생성했다. 이용약관·개인정보처리방침·면책고지 등 법적 페이지(`app/legal/`)와 세금 면책 안내 컴포넌트(`tax-disclaimer.tsx`)도 함께 작성했으며, CLAUDE.md 및 README에 빠른 시작 가이드와 환경변수 설명을 정리했다.

## diwolbu — 구독 결제일 로컬 푸시 알림 기능 구현 (25개 파일)

구독 관리 앱 diwolbu에 결제일 3일 전·1일 전 오전 9시 로컬 알림을 보내는 기능을 TDD 방식으로 구현했다. 순수함수 `computeNotifications`(schedule.ts)가 활성 구독 목록에서 알림 예약 목록을 계산하고 8개 단위 테스트(schedule.test.ts)로 검증하며, expo-notifications 래퍼인 push.ts가 권한 요청·전체 재구축·포그라운드 핸들러를 담당한다. `NotificationSync` 컴포넌트가 `subscriptions`/`push_enabled`/AppState 변화를 watch해 알림을 자동으로 재구축하고, `app/_layout.tsx`에 마운트해 앱 전역에서 동작하도록 연결했다. 아울러 Supabase 마이그레이션(00004_usage_review.sql)으로 `user_subscriptions`에 사용 점검 상태 컬럼(`usage_status`·`flagged_at` 등)을 추가하고 `savings_events` 테이블을 신설했다.

## building sns — '첫 빌딩 매수자 단골 질문 TOP5' SNS 카드 콘텐츠 제작 (23개 파일)

빌딩 중개 SNS 계정(@builpago)용 카드뉴스 9장을 제작했다. 주제는 "언제 사느냐보다 어디에 있는 어떤 물건을 얼마에 사느냐"로, 첫 빌딩 매수 희망자가 12년간 반복적으로 묻는 5가지 질문(살 수 있나·대출·어디·○○동·지금)에 대한 현장 경험 기반 답변을 담았다. card-01.html~card-09.html HTML 원본과 PNG 렌더링, Threads용 서사형 스레드·인스타그램 캡션·SEO 키워드를 포함한 본문.md, 그리고 카드 데이터를 담은 cards.json / cards-revised.json / cards-final.json을 산출물로 저장했다.

## Building scope — 세대 구획 위저드 UI 신설 및 토지이용계획 분류 보강 (3개 파일)

주택 신축 개요 조회 시 원룸·투룸·쓰리룸 등 세대 구획을 사용자가 선택할 수 있는 SPA 위저드 UI를 추가했다. `verify-dwelling-wizard.mjs`는 `parseDwellingQuery`(JSON·개별 쿼리 양쪽 파싱), SPA patcher 마커(`jitda-dw-btn`·`jitdaDwellingWizard`·`jitda_dwelling`), S2 도면 override 배선(`__dwellingChoice`)을 검증하는 스크립트다. `landuse.js`에는 V월드 NED 기반 토지이용계획(LURIS) 연동 로직과 개발제한구역(UDV100)·도시자연공원구역·공항 등 신축 원칙 금지 구역 블록 분류, 군사시설·문화유산·경관 등 NOTE 분류가 법령 근거(개발제한구역법 §12 등)와 함께 정리됐다.
