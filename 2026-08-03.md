# 데일리 작업로그 — 2026-08-03 (월)

> 전날(2026-08-03) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 6개 프로젝트, 192개 파일 변경.

## auto capcut — 하이퍼프레임 카드에 전환 애니메이션을 붙이고 시각 언어를 데이터 브리핑으로 교체 (47개 파일)
`docs/superpowers/specs/2026-08-03-hyperframe-motion-design.md`와 짝이 되는 실행 계획을 쓰고, 하드컷이던 카드 전환을 PIL 블렌딩으로 해결했습니다. 미리보기 방식을 "흐리게 미리 보이기"로 정하면 레이아웃이 고정되고 색만 바뀐다는 점에서 출발해, Playwright 재캡처나 ffmpeg `xfade` 체인 없이 상태 PNG 두 장을 섞는 `backend/render/blend.py`(이징·알파·블렌드)를 신설하고 그동안 아무도 읽지 않던 `config.hyperframe.fade` 키를 전환 길이의 단일 소스로 되살렸습니다. 시각 쪽은 글머리 점을 없애고 `statement`/`metric`/`chart`/`chapter` 4종 layout을 기존 `kind`에서 유도하도록 바꿔 analyze 스키마를 건드리지 않았고, 면·테두리·방향 색 토큰과 Pretendard 굵기 2단 임베드를 추가했습니다. 자막(STT)에 근거가 없는 숫자를 화면에 올리지 않도록 `verify_metrics` 할루시네이션 게이트를 넣었고, 프론트엔드에서는 React 편집 GUI(Timeline·VideoPreview·Segments/Subtitles/Overlays 패널, zustand store)와 순수 계산 모듈 3종(edit-ops·preview-math·timeline-math)을 테스트와 함께 정비했습니다.

## building sns — '렌트프리·관리비 착시' 카드뉴스 10장 세트 제작 (45개 파일)
매물장에 적힌 임대료와 실제 통장에 들어오는 돈이 왜 다른지를 다루는 카드뉴스 한 세트를 만들었습니다. `cards.json` 초안을 검토해 `cards-final.json`으로 확정한 뒤 generator로 인스타·스레드용 1080×1350 카드 10장과 카카오톡 소식용 `center` 버전 10장을 함께 렌더했고, cover→body→comparison→number→checklist→cta 순서에 5색 팔레트를 번갈아 배치했습니다. 내용은 무상임대(렌트프리) 조건이 매물장에 적히지 않는다는 점, 관리비가 임대료 칸에 합산 표기되면 수익률이 부풀어 보인다는 점, 그리고 계약서 특약란·입금 내역 확인 순서를 체크리스트로 묶었습니다. 비교 카드의 수치는 실제 거래가 아니라 이해를 돕기 위한 예시임을 카드 안에 명시해 두었습니다.

## diwolbu web — 8월 3일자 뉴스·프로모션·꿀팁 발행과 서비스 상세 페이지 갱신 (42개 파일)
그날치 콘텐츠로 뉴스 3건(코파일럿 프로 지원 종료, 구글의 앤트로픽 데이터센터 금융 보증, 티빙 포인트 개별 구매), 프로모션 7건(KT 콘텐츠페이의 앱스토어·구글플레이·넷플릭스·구독 부가 할인, LG U+ 구글플레이, 스포티파이 카카오페이 체험 등), 꿀팁 2건(해지 전 데이터 백업, 학생·통신사·카드 자격 할인 점검)을 추가했습니다. 뉴스 기사는 언론 보도 5건까지 출처 링크를 달아 근거를 명시하고, 결론을 "값의 방향을 맞히려 하지 말고 월간 결제로 두고 장기 약정은 확신이 있을 때만"이라는 구독자 행동 조언으로 연결했습니다. 함께 `lib/details/` 아래 웨이브·지니·어도비·클래스101·요기요·이모티콘플러스·세이보카·v0·아이디오그램·매그니픽 등 서비스 상세 데이터를 2026년 8월 기준으로 갱신해 요금제 구성, 장단점, 대체 서비스 비교, FAQ를 손봤고 `lib/plans.ts`·`lib/cancel.ts`도 같이 정리했습니다.

## Building scope — 조경·분할제한 기준의 지역 분기, "서울 기준을 전국에 적용"하던 결함 수정 (33개 파일)
법제처 국가법령정보 Open API로 건축법 §42①·시행령 §27(조경)과 법 §57·시행령 §80(분할제한) 원문, 그리고 서울·부산·대전·성남 조례를 대조해 `docs/legal/`에 대조 문서 2건을 남겼습니다. 그 결과 `lib/landscape.js`가 서울시 건축조례 §24의 조경 비율을, `lib/buildability.js`가 서울 조례 §29의 최소 대지면적 표를 전국에 무분기로 적용해 비서울 필지에 과대·과소 산출과 거짓 인용이 붙던 문제를 찾아 `isSeoul` 게이트를 넣고, 시·도 판정을 `lib/region.js` 하나로 이관해 세 모듈이 같은 규칙을 쓰게 했습니다. 프로젝트 원칙대로 근거가 없는 구간은 숫자를 만들지 않고 '미상'으로 두는 쪽을 택했으며, `verify-landscape-region`·`verify-subdivision-region` 등 검증 스크립트 7종으로 회귀 0을 확인했습니다. 콘텐츠 쪽으로는 스레드 글 5단 템플릿과 정직성 가드레일을 담은 포맷 가이드, 그리고 주차장 기준·토지 매입 체크리스트·상가주택 층수 규정을 다룬 블로그 큐 원고 3편과 스레드 요약본을 함께 정리했습니다.

## howmuch.go — 강남권 신규 필지 1건의 임대·매매 시세 리포트 생성 (13개 파일)
새로 요청된 필지 한 곳에 대해 네이버 임대·매매 원자료와 국토부 실거래 자료를 받아 캐시한 뒤, 임대는 `clusters.json`, 매매는 `sale.json`으로 정규화했습니다. 대상지의 건축물대장 정보(대지·연면적·건폐율·용적률·준공연도·지상지하 층수·주차대수)를 `subject.json`으로 저장하고, 임대·매매 각각의 리포트 HTML과 지도 이미지를 생성했습니다. 마지막으로 `data/manifest.json`에 주소↔슬러그 매핑과 모드별 갱신일을 추가해, 기존에 쌓여 있던 30여 개 필지 목록에 이번 건을 합류시켰습니다.

## blog auto — 매각 사례 PDF에서 블로그 원고와 카드 이미지 자동 생성 (12개 파일)
`input/`에 넣은 물건 브리핑 PDF 한 건을 분석해 네이버 블로그용 원고를 `draft.html` → `post.html` 순으로 뽑고, PDF에서 추출한 본문 이미지 5장을 함께 배치했습니다. 소재는 종로구 관철동 종각역 인근의 지상 11층 숙박시설 건물로, 39실 직영 운영이라 임차인에게 나눌 몫 없이 매출이 소유주에게 남는다는 점과 건물가액이 값에 잡히지 않고 순수 토지값으로 거래된 구조를 물건개요 표와 함께 짚었습니다(구체 매매가·평당가·수익률 수치는 원고 안에만 담았습니다). 여기에 `thumb.html`과 `reasons.html`을 렌더해 썸네일과 "이 건물이 팔린 이유 3가지" 카드 이미지까지 생성했습니다.
