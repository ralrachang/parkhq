# 데일리 작업로그 — 2026-07-14 (화)

> 전날(2026-07-14) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 4개 프로젝트, 210개 파일 변경.

## sise-web — 국토부 실거래 캐시 일일 자동 갱신 배치 설계·구현 (130개 파일)
서울 25개 구의 국토부(MOLIT) 상업업무용 실거래 캐시를 워커 유휴 시간에 매일 자동 갱신하는 배치를 설계부터 구현까지 완료했다. 설계 스펙과 태스크별 TDD 플랜(molit-trade-refresh)을 먼저 작성한 뒤, molit.py에서 월 단위 수집 함수 `fetch_month(force=)`를 추출해 임시파일→`os.replace` 원자 교체를 적용하고, 신규 모듈 `refresh_trades.py`에 구 단위 갱신(`refresh_gu`)·SQLite meta 기반 일일 진행 상태(`maybe_refresh`)·수동 CLI를 구현해 worker.py의 idle 사이클에 배선했다. 전용 테스트(test_refresh_trades.py)를 신설해 스위트를 확장했고, 실제 실행으로 종로구를 비롯한 구별 월간 실거래 캐시 JSON이 대량 갱신됐다. README·RESUME 문서를 갱신하고 PLAN2-TODO에 최종 리뷰 이월 항목(실패 구 회전, 임시파일명 충돌, 배포 시 KST 타임존 고정 등)을 기록했다.

## building sns — '눈에 안 보이는 프리미엄' 카드뉴스 9장 제작 (41개 파일)
"엘리베이터·주차·코너가 임대료를 정한다"는 주제의 카드뉴스 1편(hidden-premium)을 제작했다. cards.json 초안 작성 → 콘텐츠 리뷰 반영(cards-revised.json) → cards-final.json 확정 흐름을 거쳐 인스타/스레드용 카드 PNG 9장(1080×1350)과 카카오톡 소식용 가운데 정렬(center) 버전 9장을 생성했다. 스레드 토막글·인스타 캡션·SEO 키워드·데이터 출처와 한계 주석까지 담은 본문.md도 함께 작성했다.

## diwolbu web — 이미지·디자인 AI 상세 페이지 5종 추가 + 이틀치 콘텐츠 발행 (31개 파일)
구독 서비스 상세 콘텐츠에 이미지·디자인 AI 5종(Leonardo AI, Ideogram, Magnific AI, Envato Elements, Adobe Creative Cloud)의 상세 페이지를 신규 작성하고 레지스트리(lib/details/index.ts)·서비스 목록(services.ts)·요금 데이터(plans.ts)에 등록했다(ChatGPT 상세도 함께 손질). 콘텐츠 쪽은 당일자 꿀팁 「1월에 결심한 영어 공부 구독, 7월에도 결제만 되고 있다면」(스픽·말해보카·클래스101 점검)과 뉴스 「클로드 페이블 5 무료 포함 세 번째 연장」을 발행했고, 다음 날(7/15)자 뉴스 3건(바이트댄스 Seedream 5 Pro, KT 콘텐츠 구독 할인, 밀리 오디오북 종료)과 꿀팁 1건도 미리 준비했다. 이와 함께 6~7월에 발행된 기존 팁·칼럼 JSON 다수를 함께 갱신했다.

## howmuch.go — 캘리브레이션 판정 축 band_hit→MdAPE 이관 (8개 파일)
매매가 추정 백테스트의 신뢰등급 캘리브레이션 판정 축을 밴드 적중률(band_hit)에서 등급별 점오차 중앙값(MdAPE) 비교로 이관했다. 설계 스펙과 TDD 플랜을 작성한 뒤 pipeline/backtest.py에서 by_score 셀에 타깃별 APE를 기록하고, calib_pool이 자격 등급 중 최상·최하의 MdAPE를 비교해 판정하도록 교체했으며 tests/test_backtest.py를 새 스키마로 갱신했다. `backtest --all` 검증 실행으로 기존 골든 수치 무손상과 캘리브레이션 verdict의 violation→ok 전환을 확인하고, 정확도 스펙 §5 개정과 ★검증 기록(백테스트 캐시 JSON 2건)을 문서화했다.
