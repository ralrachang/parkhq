# 데일리 작업로그 — 2026-08-24 (월)

> 전날(2026-08-24) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 5개 프로젝트, 150개 파일 변경.

## diwolbu web — 하루치 콘텐츠 발행 + 구독 요금 카탈로그 전면 재검증 (62개 파일)
구독 관리 서비스 '디월부'의 에디토리얼 파이프라인으로 8월 24일자 발행을 돌렸습니다. 뉴스 4건(엔비디아 AI 서버 가격 인상, 오픈AI가 차기 모델 '아스트라' 훈련을 스스로 중단한 건, 유튜브 프리미엄 유럽 인상, 카카오뱅크·신한 구독 할인 카드), 꿀팁 1건, 프로모션 신규 2건(SKT 휴대폰결제의 애플·MS 청구할인, 구글플레이·PS스토어 청구할인)을 새로 썼고 다음 날 나갈 칼럼 「인상은 배달되고, 인하는 찾아가야 합니다」까지 함께 작성했습니다. 발행과 별개로 프로모 기한 점검을 돌려 공식 API에서 종료 확인된 이벤트 1건을 내리고 26건의 근거·재확인일을 갱신했으며, 콘텐츠 감사에서 클래스101 연 구독료·넷플릭스 추가 회원 구분·어도비 해지 위약금(미국 규정을 한국 약관으로 잘못 적고 있던 것) 등 15건의 사실관계를 공식 출처와 대조해 정정했습니다. 카탈로그 코드(`lib/details/*.ts` 7종·`lib/cancel.ts`·`lib/plans.ts`)도 같은 기준으로 손봤고, 캔바 요금은 감사 1차 판정이 오독이었음을 라이브 요금 페이로드로 확인해 원래 값으로 되돌렸습니다.

## senior youtube — 영상 생성 API 검증 + 히스토리 쇼츠 1편 완성 (46개 파일)
쇼츠 자동 제작 파이프라인(factory)에서 '민스미트 작전' 편을 대본부터 완성본까지 한 번에 뽑았습니다. 먼저 Higgsfield API로 3종 테스트(지도 라벨·봉랍 봉투 라벨·안전필터 통과)를 돌려 결과 클립과 컨택트시트를 남겼고, 지도 샷에 '9:16'이라는 글자가 화면에 그대로 그려져 나오는 현상을 두고 "프롬프트의 비율 문구가 찍히는 것"과 "Lite 티어라 배경 텍스트가 무너지는 것" 두 가설을 각각 검증하는 재현 스크립트(`hf_test2.py`)를 작성했습니다. 이어서 샷 10개 프롬프트 작성 → 클립 수집 → TTS 내레이션 → SRT/ASS 자막 → 합성까지 태워 63초짜리 세로 영상과 커버·10샷 검수판을 만들고, 제목·설명·태그·해시태그가 붙은 검수 요청서를 출력했습니다. 코드 쪽은 대본을 문장 단위로 쪼개는 파서(`script_parser.py`)와 발화 호흡에 맞춰 자막을 끊는 로직(`subtitles.py`)을 다듬었고, 채널 설정의 `keep_together` 목록에 '민스미트 작전'·'해병대 소령' 등 갈라지면 안 되는 어구를 추가했습니다.

## Building scope — 규모검토 엔진 정확도 결함 3건 동시 수정 + PDF 오버플로우 (28개 파일)
실제 주문 필지(수원 지동)를 처리하다 한꺼번에 드러난 정확도 결함 세 가지를 같은 날 잡았습니다. ① 도시·군계획시설 저촉을 '고지만' 하던 것을 V월드 계획시설 폴리곤과 지적 폴리곤을 교차시켜 실제 저촉 면적을 재고 대지면적에서 차감하도록 바꾸고(신규 `lib/plan-facility-area.js`, 도로후퇴와의 이중 차감 방지 포함), ② 주택 용도를 '다세대주택'으로 하드코딩하던 것을 시행령 [별표1] 원문 대조 기반 판정 모듈(`lib/housing-kind.js`)로 분리해 단독·다가구·다세대·연립을 구분하고 위저드에 선택 항목을 신설했으며, ③ 서울시 조례값만 갖고 있던 건폐율·용적률·주차 기준에 수원시 조례를 추가했습니다(`lib/ordinance-zoning.js`·`lib/ordinance-parking.js`). 여기에 LURIS 관측 코드와 엔진이 실제로 소비하는 코드의 차집합을 뽑는 sweep 도구(`scripts/sweep-luris-consumed.mjs`)를 만들어 아무 고지도 하지 않고 있던 코드 2종(건축허가·착공제한지역, 교육환경보호구역)을 찾아냈고, PDF 렌더에서 법규체크 경고 카드와 주차 근거 문구가 시트 밖으로 밀려 경고가 통째로 사라지던 결함도 고쳤습니다. 신규 검증 게이트 2종을 포함해 오프라인 게이트 전수 실패 0을 확인한 뒤 핸드오프 문서(`TOMORROW.md`)를 현행화했습니다.

## blog auto — 매물 PDF → 블로그 포스트·카드뉴스 자동 생성 1건 (13개 파일)
빌딩 매물 자료 PDF를 넣으면 블로그 원고와 이미지 카드가 나오는 파이프라인으로 강남구 신사동 근린생활시설 사례 1건을 처리했습니다. 입력 PDF에서 물건개요·수익 지표·현장 사진을 추출해 초안(`draft.html`)과 최종 포스트(`post.html`)를 만들고, PDF 페이지에서 뽑은 사진 4장에 썸네일·CTA 이미지를 붙였습니다. 표지 카드(`thumb`)와 '매수 이유' 카드(`reasons`)는 다크네이비·골드 톤 템플릿을 HTML로 렌더한 뒤 이미지로 굽는 기존 방식을 그대로 재사용했습니다. 본문은 물건개요 표 → 수익성 분석 → 매수 판단 근거 순의 서술형으로 구성했고, 구체 거래 지표는 원문 자료 기준값을 그대로 옮겨 적었습니다.

## wonbuilding AI TF — AI 구독 비용 지원 정산표 통합 (1개 파일)
사내 AI TF의 AI 구독 비용 지원 건으로, 그동안 월별로 흩어져 있던 결제내역 파일을 5~8월 4개월치를 한 번에 담은 지급정산용 통합 시트로 다시 만들었습니다. 직원별·월별 결제 항목을 한 표에 모으고 월별 열과 합계 열을 붙여, 지출결의 문서에 그대로 첨부할 수 있는 형태로 정리했습니다. 대상자와 개별 금액은 사내 정산 자료라 이 로그에는 옮기지 않습니다.
