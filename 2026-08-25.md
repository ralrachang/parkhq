# 데일리 작업로그 — 2026-08-25 (화)

> 전날(2026-08-25) 선택 폴더에서 파일 변경이 있었던 프로젝트를 자동 감지해 정리했습니다. 총 4개 프로젝트, 71개 파일 변경.

## senior youtube — 세계사 쇼츠 7편 「에펠탑」을 클립 생성부터 검수까지 완주 (36개 파일)
힉스필드 CLI로 11개 샷을 한 번에 뽑아 `clips_inbox/20260826_eiffel/` 까지 내려받는 흐름이 처음으로 사람 손 없이 돌았고, 안전필터에 걸린 클립 없이 11장 전원이 통과했습니다. 이번 편에 맞춰 `factory/config/visual_style.json` 의 `base_look` 에서 화면 비율을 지시하던 문구를 영구 제거했는데, 프롬프트에 비율을 적으면 모델이 그걸 화면에 그릴 텍스트로 오해한다는 가설이 11장 전부에서 확인됐습니다. `work/20260826_eiffel/` 아래로 `job.json`·`prompts.md`·자막(`.ass`/`.srt`)·나레이션·세그먼트 11개·`video_only.mp4` 까지 산출물이 쌓였고, 검수 통과 뒤 제목·설명·태그를 담은 `업로드용.txt` 로 업로드 문안을 확정했습니다. 루트의 `진행상황.md` 에는 시대 고증이 어긋난 샷 07처럼 그대로 가기로 한 잔여 결함, 클립 생성 도구 구독을 정리한 뒤의 잔여 크레딧 운용 방침, 다음 편 주제를 클러스터로 묶어 고르자는 제안을 이어서 읽을 수 있게 정리해 뒀습니다.

## yangjae_NI — 대치동 건물 적정 매매가 추정 파이프라인과 보고서 일습 구축 (28개 파일)
수집 단계를 스크립트로 잘게 나눠 짰습니다. `fetch_molit.py` 는 국토부 상업업무용 실거래(RTMSDataSvcNrgTrade)를 강남구 기준 48개월치 받아 대치동만 추려 내고, `fetch_bldg.py`·`fetch_floors.py` 는 건축물대장 총괄·층별개요로 본건의 면적과 층별 용도를 확보하며, `fetch_naver.py` 는 Playwright로 네이버페이 부동산 지도 API를 워밍업한 뒤 반경을 타일로 쪼개 건물 매매 호가와 상가·사무실 월세를 모읍니다. 이어 `process.py`·`match_geocode.py` 로 좌표와 대지면적 기준 중복을 정리하고, `analyze.py` 에서 본건 기준 반경별·대지면적대별·신축 여부별 평당가 분포와 수익환원법을 함께 돌렸으며, `rent_check.py` 로 현행 임대료가 인근 시세 대비 어느 수준인지 지하·1층·2층 이상으로 나눠 검증했습니다. 마지막으로 `build_map.py` 가 실거래와 호가를 두 레이어로 얹은 folium 지도를, `build_report.py` 가 Pretendard 흑백 양식의 HTML 보고서를 생성해 PDF 출력본까지 만들었습니다. 구체적인 추정 금액과 가격 밴드는 보고서 원본에만 두었습니다.

## diwolbu web — 프로모션 2건 발행 + Envato·Vrew 카탈로그 수치 정정 (5개 파일)
'카카오뱅크 착붙 신한카드'와 'SKT 휴대폰결제 멤버십 플래티넘 앵콜 프로모션' 두 건을 `content/promos/` 에 새로 넣었습니다. 두 건 모두 혜택만 적지 않고, 전월 실적 조건·공식 홈페이지 정기결제만 대상이라는 제약·다른 이벤트와의 중복 참여 제한·자동 해지되지 않는 유료 부가서비스라는 점 같은 함정을 `note` 에 따로 짚고 공식 보도자료와 상품 페이지를 출처로 달았습니다. 카탈로그 쪽에서는 Envato Elements가 2026년 8월 AI 크레딧제로 바뀐 것을 반영해 티어 차이를 'AI 생성 횟수'에서 '크레딧 양'으로 고치고, 최상위 Ultimate 요금이 낡아 있던 것을 공식 요금 페이지 기준으로 바로잡았습니다(`lib/plans.ts`, `lib/details/envato.ts`). 같은 커밋에서 Vrew 요금이 `cancel.ts` 한 곳만 다른 파일과 어긋나 있던 것도 맞췄고, 타입체크·테스트·빌드를 모두 통과시킨 뒤 커밋했습니다.

## wonbuilding AI TF — 베스핀글로벌 용역계약·NDA 상대측 수정안 접수 (2개 파일)
AI 에이전트 구축 프로젝트(NL2SQL 데이터 조회 에이전트, 매칭/RAG, WCD 연동, 인프라 확충)를 대상으로 한 '용역/컨설팅 계약서'와 '비밀유지계약서(NDA)'에 대해, 상대측이 변경이력을 켠 채 회신한 수정안 두 건이 폴더에 들어왔습니다. 손댄 양은 용역계약서 쪽이 압도적으로 많아 삽입·삭제 표시가 백 군데를 넘고, 투입 인력의 등급과 교체 절차·제3자 재위탁 제한·검수와 승인 간주·하자보수 기간과 조치 기한·결과물의 지식재산권 귀속 같은 핵심 조항 전반에 걸쳐 있습니다. NDA는 상대적으로 수정이 적고 비밀정보의 범위와 다른 관련 계약과의 우선순위 조항 위주입니다. 계약 기간·용역대금·지급 조건 같은 수치는 문서 원본에만 두고 이 로그에는 옮기지 않았습니다.
